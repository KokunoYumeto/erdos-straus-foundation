# Turn 7: completed trace return and remaining existence obligation

The determinant theorem already covered mod24; do not redo it as though
mod12 meant exclusion of the mod24 subprogression.

Completed here:
- Original rational integral-tail-trace sources are kept before full gates.
- Every E trace on one of the nine primitive rays rs|6 has a full squarefree
  ray return and a marked M return at the same p. All trace fibres and their
  original multiplicities are explicit, with the strict R<p boundary.
- Every such rational trace at a=cq,c|6,q>3 prime gives M at the same p.
  E uses ray-preserving transport; M uses a small original word. The distinction
  is substantive: old fixed-word deletion fails in the p1129 E example.
- Automatic ray-preserving residual-divisor repair is optimal: every other
  coprime ray has an infinite reduced hard-prime progression of proper sources
  where ray and fixed-word returns both fail. The E complement also fails the
  relaxed gate. These primes have actual endpoint integer solutions.

Unproved: that every prescribed hard prime has a first trace in this family,
or any original integral coefficient elsewhere. At p2521 and3361 the entire
mixed prime-shell trace family is empty. At3361 all nine automatic rays are
empty too, although a841=29^2,R3,u29 is a genuine state.

Permanent controls:
- p37 disproves dropping the mod24 carrier-character assumption.
- p9601,a2956=4*739,u8 has a proper M trace; its squarefree target a2462,R247
  has BOTH original gates empty. No automatic extension to c4 is proved.
- p2689,R1863 has squarefree part23 and radical69; the latter is the wrong type.
- p6975049201 has an original proper trace on ray(5,3), but the ENTIRE genuine
  ray and both original gates of its selected shell fail. This is not global
  ES failure: a1902286146,u3 is an independent endpoint M state.
- A split integer coefficient map with section t=1 can have an empty target.
- Dirichlet is used only on explicitly reduced linear progressions. No prime
  values of nonlinear equality polynomials are assumed.

The next useful arithmetic task is to enlarge the source-changing mechanism
beyond the prime-carrier character split, or to force a full pair coefficient
on a genuinely universal source. One needs actual factor data coupling different
shells at the same prime. More determinant invertibility or unrestricted unit
square assertions do not discharge that occupancy step. No Turn8 witness-guarantee
is being inferred from these reductions.
