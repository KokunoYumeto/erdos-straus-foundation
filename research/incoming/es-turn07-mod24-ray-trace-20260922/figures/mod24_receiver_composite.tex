\begin{figure}[ht]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  >=Latex,
  box/.style={draw,rounded corners,align=center,inner sep=5pt,text width=34mm},
  map/.style={->,thick},
  note/.style={align=center,font=\small,text width=43mm}
]
\node[box] (trace) at (0,0) {marked rational trace\\
\(T=(p,c,q,a,y,z)\)\\
\(y+z\in\mathbb Z\)};
\node[box] (factor) at (4.5,0) {exact factorization\\
\(R,u,h,r,s,d\)\\
E or M channel};
\node[box] (eray) at (9,1.9) {E branch\\
\(R=\delta t^2,\ rs\mid6\)\\
\(H=(\delta+1)/(4rs)\), \(J=(pr+s)/\delta\)};
\node[box] (mword) at (9,-1.9) {M branch\\
marked complement\\
\(u_0\mid c^2\mid36\), \(a'=(p+\delta)/4\)};
\node[box] (witness) at (13.5,0) {integral M witness \(W=\rho(T)\)\\
E input: \((HsJ,pHsr,pHJr)\)\\
M input: \eqref{eq:M-return-sorted}};
\node[box] (receiver) at (18,0) {literal receiver\\
\(\mathscr O(W,\xi):V_{\mathrm{labels}}\to E_{\mathrm{recv}}\)\\
invertible with \eqref{eq:bridge-inverse}, \eqref{eq:bridge-compounds}, and
\eqref{eq:bridge-det-max}};

\draw[map] (trace) -- node[above,font=\small] {Lemma~\ref{lem:trace}} (factor);
\draw[map] (factor) -- (eray);
\draw[map] (factor) -- (mword);
\draw[map] (eray) -- (witness);
\draw[map] (mword) -- (witness);
\draw[map] (witness) -- node[above,font=\small] {\(\mathscr O\)} (receiver);

\node[note] (fibre) at (9,-4.5) {For fixed \(p,r,s,\delta\), all allowed
\(t\) map to the same witness; retaining \(t\) recovers the source.};
\draw[map,dashed] (eray.south) -- (fibre.north west);
\node[note] (boundary) at (18,-4.5) {No reverse arrow is proved:
receiver invertibility does not create the initial trace source.};
\draw[map,dashed] (receiver.south) -- (boundary.north);
\end{tikzpicture}
}
\caption{The typed composite from a marked mod-\(24\) rational trace to an
integral witness and then to its literal receiver.  Both arithmetic branches
are displayed.  The dashed fibre annotation identifies exactly the source
data collapsed by \(\rho\), and the missing reverse arrow is recorded rather
than assumed.}
\label{fig:mod24-receiver-composite}
\end{figure}
