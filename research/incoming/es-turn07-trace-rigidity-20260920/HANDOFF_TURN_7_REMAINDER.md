# Turn 7 remains the original existence task

Established here:
1. For n rational roots with first n-1 integral traces, all reduced denominators
   coincide, and d^n divides (n-1)! times the reduced reciprocal numerator.
2. With reciprocal sum 4/p and n>=3, these hypotheses force d=1. For ES, two
   global integer traces suffice. This is not a claim that such traces occur.
3. The exact three-root denominator types and sharp numerator valuations are
   classified and constructed. The numerator-four restriction is substantive.
4. On the complete raw *integer* sum-gate source, one trace is integral exactly
   when U|p*a^2. Correctly recover E at U=p*u; do not mislabel it M.
5. Actual trace-fibre collisions are exactly middle complements. The original
   coefficient kernel has basis e_U-e_(a^2/U) and rank |M|/2, without a factor
   inventory assumed as input. Its possible nonzero rank has not been forced.
6. One trace on arbitrary rational tails leaves precisely a square-part residual
   defect. At p1009 the trace-integral R135 source has an empty compressed R15
   original shell. No square-stripping witness map follows.
7. At every hard p there are positive irrational algebraic-integer targets with
   *all* integral power traces, actual local M clusters, and the scalar bounds.
   They are divisors in a quadratic integer ring, not in the original Z source.

Useful next exact target:
For some a in the complete first-half range, prove that the raw trace image
contains an integer, or prove a nonzero kernel of the specified free-abelian
trace-label map. Either condition has an original E/M return. An arbitrary
nilpotent, an analytic trace, or a scalar extension of the raw word ring is not
that map. The positive first two-trace criterion only applies after rationality.

Permanent controls:
- (1/3,1/3,4/3) has integral first two traces but reciprocal numerator27, not4.
- n2, (p/2,p/2), defeats dropping n>=3.
- p1009,a286,R135: rational tails6413/3,168238642/3; trace1 integer, trace2 /9.
  The compressed R15,a256 source has both original gates empty.
- p1009,a253,U11099 is an E state after removing p, not a canonical M word.
- p13,a4,U5: trace436/5 achieves distance1/U; retain strict approximation signs.
- p35281,a8821,U202883: the prior foreign-prime candidates have trace denominator23.
- p1009 algebraic template: a253,k15148, Delta2049827360 lies strictly between
  45275^2 and45276^2. All moments are integers but the quadratic roots are irrational.

The complete original source is still not proved occupied for every hard prime.
Do not announce a witness-guaranteed Turn 8 procedure or universal ES coverage.
