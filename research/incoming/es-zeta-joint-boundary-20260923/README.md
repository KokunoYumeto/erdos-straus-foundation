# ES continuation from full-support Split-Zero arithmetic

Source cut: Zeta `8fea210da131ef7714e54eea6becc876c4a55320`, editions
031 PD1-10 and IA1-9, and 032 FSJ1-7 / FLC6-21. ES base:
`20709bee872ad10e4cc7d7b976d975e24532a803`.

The self-contained three-page `preprint.tex` proves an unbounded original
mixed-support packet family, its exact bounded digit return with retained
boundary, a finite torsion/relative-action crosswalk, and an affine two-channel
consequence. The latter supplies an actual same-shell E return at p=6841 from
a two-word packet with empty M gate.

The proof's infinite prime families use Dirichlet only after every linear
progression is explicitly proved reduced. They do not cover every prescribed
prime. A full original failure object is not replaced by one selected packet.
No RH/Weil positivity, independent human review, or priority claim is made.

Run with standard-library Python 3.10+:

```sh
python compact_verify.py
python -O compact_verify.py
pdflatex -interaction=nonstopmode -halt-on-error preprint.tex
pdflatex -interaction=nonstopmode -halt-on-error preprint.tex
```

The compact checker replays all stated isolated primality certificates,
the complete 1215-word and 135-word surrounding E/M boxes, the high-order
packet counts for 2<=n<=8, the paired-channel return, and bounded exact
mass-boundary/radix tests. It uses exceptions, not disabled assertions.
The prime certificates are recursive full-order n-1 certificates with trial
leaves below 100. No external numerical or symbolic package is required.

These files are the public proof note and compact certificate, not the full
historical cumulative workbank. The expanded ten-page integration proof and
larger 5,616,298-word replay are a separate generated handoff. The ordinary
radix and cyclic integration facts are elementary tools, not purportedly new
abstract algebra. The universal Stage 7 ES occupancy step remains open in
this continuation.

Collective byline: The Clankers. No prior repository file is replaced.
