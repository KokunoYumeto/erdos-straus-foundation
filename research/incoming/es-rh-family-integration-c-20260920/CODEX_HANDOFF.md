# Handoff: general-family integration C

Start with README.md, then RESEARCH_CONTINUATION.md, CLAIM_REGISTER.json, and results/VERIFICATION_SUMMARY.json. Replay with python checks/run_validation.py.

1. Preserve the received ZIP, pasted result, and GF source without modification. The corrected live proofs are separate. Keep GF labels distinct from MR, JS, ER, and CP.
2. MR7–MR10 and MR14 compare the constructed section metric with the canonical metric on the same cyclic algebra using NG20–NG21 at the exact provider blob in SOURCE_LEDGER.md. The complete divisor, source measure, physical section scalar, and separate \(s=0\) measure are retained.
3. JS1–JS15 is the original-source return. Compute the full \(C_X\), \(H_Z\), and \(F\) from one source and verify \(\ker H_Z\subseteq\ker F\). For \(\mu=\lambda_{\min}^+(H_Z)\), use a certified \(0<\underline\lambda\le\mu\); do not reverse that quantifier. For observations, set \(K=\ker\Lambda\) and retain the \(T_{12}\) Schur term.
4. ER5 and ER10 combine the ES generator with primary-length mixing. ER7 is a projector only on the generated top-chain sector \(\mathscr T\), not the ambient tensor algebra. ER13 assumes \(k\ge2\); its \(k=1\) boundary is stated separately.
5. CP2–CP3 evaluate the weighted-minor minimum for every real \(a\ge0\), with the transition at \(a=1\). Retain fixed uniformly equivalent input and residual target metrics. At rank \(r\), coordinate changes use \(\|\wedge^r C^{\pm1}\|\). Restore every physical target multiplier.
6. MORPHISMS.md records the exact maps. The present bridge to trace rigidity is the common-source span from an existing ES witness. A direct target-to-target map still requires the four specified arrows and their fibre, kernel, and Gram proofs.

Next calculations: evaluate the native source covariance and observation with outward moment errors; insert the larger ER value space into an explicitly typed original observation; construct an arithmetic deformation into the CP collision coordinate; and test whether the trace involution factors through the common-source span. None of these missing calculations is assumed by a theorem in this module.

The repository publication receipt is the commit that adds this module and stable results SZ-20260920-063 through SZ-20260920-066.
