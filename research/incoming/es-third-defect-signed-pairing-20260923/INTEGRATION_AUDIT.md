# Integration and proof audit

## Received objects

The supplied PDF and program are preserved byte-for-byte under `received/`.
Their hashes and original locators are in `RECEIVED_INPUT.json`. Source TeX
was not among the received files, so `core.tex` is a reconstructed and
extended proof document rather than a claimed recovered source.

## Independent mathematical findings

The same-shell third-defect theorem, its count identity, the increasing-triple
count, the applicable-residual classification, and the residual-27
factorization theorem all survived independent derivation.

The program required two schema corrections:

1. its marked tuple was not always in increasing order;
2. after a complemented return, one exponent field described the input rather
   than the returned word.

The active copy now retains both marked and increasing tuples, and both input
and output centred exponents. The received file remains unchanged.

The signed pairing survived a separate exhaustive fibre derivation. The
orientation of the mixed fibre is now explicit: `r` is read from the
`p`-bearing divisor. The real-`sigma` hypothesis, orthonormal counting
inner product, exact-order unit stratum, unscaled degree operator, and global
rather than one-stratum action are all stated.

The cross-shell calculation required the unit cancellation
`gcd(d,R)=1`. This follows in the stated original-shell domain from
`d|pa` and `gcd(pa,R)=1`; it is now proved before `mu` is declared
integral. Without this condition, `mu` need not be integral.

## New structural deductions

The received third-defect theorem is extended to every odd quotient
`N=R/m`. The exact uncovered-orbit term is retained, and the map to the prior
affine fine coordinate is proved. Three is the unique nontrivial odd quotient
with universal complement closure. The converse is sharp: every odd `N>3`
has an explicit reduced prime progression with a wholly uncovered orbit.

The failed cross-shell fusion is retained as a cyclic obstruction class. Its
order is the reduced numerator that remains in the four-term identity. This
defines the exact failure space of the prescribed fusion without asserting
that other returns do not exist.

## Replay

`python run_all.py` passes. The active checker reports 167 assertions,
including 90 exact rational signed-pair comparisons and direct algebraic
checks of the reduced prime progressions for
`N = 5, 7, 9, 11, 15, 21`. The independent checker imports no active
implementation and reconstructs all four displayed residual-27 packets, the
complete residual lists for the controls, and the cross-shell obstruction.

## Nonclaims

No universal Erdős–Straus theorem, counterexample, human peer review, Lean
proof, or historical-priority conclusion is claimed. The unresolved global
step is source nonemptiness or cross-shell obstruction elimination for every
prescribed hard prime.
