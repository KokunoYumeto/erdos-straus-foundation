# Turn 6 handoff: the exact original sum, not another restricted packet family

## Established and retained

1. In an original shell R=4a-p, every square divisor u has Jacobi(u/R)=Legendre(u/p). E hits require chi(u)=-1. M hits require chi(u)=-chi(a). Thus for every negative-character square source, E and M require opposite characters. Fixed-u and square-ratio maps cannot change channels there.
2. The same-source bounded ratio map is exact: f' = f+delta with max(0,-delta)<=f<=min(2e,2e-delta). M -> E requires ratio p^-1 modulo R; E -> M requires p. No availability is inferred merely from a residue equation.
3. One original p-coloured pair source P_R(N)={b,c|pN : R|b+c} has exact fibre 2*tau(h) over each primitive M/E pair. M has a common p-bit, E a p-side bit. The original divisor and denominator return are attached only at 4N=p+R.
4. F_R(N)=1/2 sum_{d|N} mu(d) T_R(N/d). At N=a this is E+M. Hence C_p is the sum over ALL first-half shells, not a dyadic or graph-only substitute.
5. The character formula is F_R(N)=1/(2phi(R)) sum_chi chi(-1)|1+chi(p)|² product_{q^e||N} sum_{j=-e}^e chi(q)^j. At every negative square source its transported Jacobi character has chi(p)=-1 and vanishes in this expression. The character forcing factor expansion is therefore not a positive term for the desired count.
6. The classical reciprocal source (pD+1)/4 with a sum gate returns first-half E states with fibre two. Its full finite D bound for a fixed p is floor((p²+6p+13)/12). Keep the original residual R=(4u+1)/D and the pD versus p+D distinction.
7. Common nonresidue factors of (p+D)/4 and (pD+1)/4 over 0<D<3p give exactly the inherited prime-3-mod-4 factor of p+1 route. Do not present this as new universal progress.

## Negative route-selection control

At p=12889, q=43 and q=61 have 22 valid square ports. Every one of the 22 original E/M sources and every one of the 22 reciprocal sum-gate sources is empty (246+600 original divisor vectors). The canonical reciprocal sources also have no nonresidue factors, so they do not repair the canonical two-cycle. This is NOT a closed component under the whole square-source graph. Some reciprocal factors exceed p and are not original vertices.

## Surviving exact interaction

At R=31,a=3230, the original M divisor 25 and E divisor 85 differ by 17/5, a non-square character-changing ratio in the actual exponent bounds. The reciprocal source q=61,t=11 has nonresidue factor 31 and reaches this source. The ordered solutions are (3230,174852174,1353345) and (3230,1346910,456850605). This is an example and a typed map, not a universal eventual-return theorem.

## Turn 6 obligation

Start with an arbitrary hard prime. Attempt a positive lower bound for the complete C_p, or construct a genuinely universal forcing map. The preceding five turns do not imply that lower bound. Do not count an equivalent finite expression, a formal positive Gram, a bounded successful scan, or another favourable prime-factor family as its proof.

The exact sum now retains the common-divisor Möbius cancellations and the four p-colour terms. Bounding every term independently in absolute value may erase the useful signed interaction. Work on these original coefficients or return an explicit reason the proposed estimate fails. All auxiliary N=a/d sources remain nested original divisor pairs; they are not falsely made into shell equations.

## Evidence boundary

Read verification.json for actual completed runs and bounds. No old large Turn 4 cofactor proof is replayed by this tranche; the predecessor is preserved rather than silently re-certified. No remote publication is inferred from a local patch.

## Simpler positivity target without losing the inverse
The exact fibres also prove 2 F_R(a) <= T_R(a) <= 2 tau(a) F_R(a). Therefore C_p>0 iff sum_{p/4<a<p/2} T_{4a-p}(a)>0. Turn 6 may force one original p-coloured pair directly and then use the gcd inverse. The Möbius transform is needed for exact counts, not as an additional existence hurdle. Keep this simpler equivalent target alongside the signed character expression; do not require termwise positivity of Möbius summands.

The full shell sum includes positive-character sources as well. At R|(p-1) the original E/M gate sets coincide (possibly both empty); otherwise their same-u intersection is empty. Example p1009,R3,a253,u11 gives both labelled solutions (253,87032,3818056) and (253,2042216,88792). The nonresidue-source graph restricts chi(a)=-1 and omits this positive-source mechanism. Do not carry its channel separation beyond its stated domain.


## Added positive channel theorem and its exact limits

The recovered unfinished draft has now been checked and extended. The original
quarter involution u -> a/(4u) crosses E/M on Div(a/4), not on arbitrary square
divisors. Its ratio has chi_p(a), so it is a real character-changing return on
negative sources. The exact two windows for a=2^eA, e>=1, are[-2e-2,-2] and[-e,e].
Their union has length3e+3. With a unique simple nonresidue factor and a positive
prime-power remainder, these windows give the COMPLETE two-channel count; the
two equal-length counts E andM/2 differ by at most1 when4 lies in the cyclic group.
This is not full coverage if -r is in another coset or its target lies outside
the original available intervals. p23209,a=2*2917,R127 is the exact one-class hole.

The new p315361 canonical six-cycle is
27847->42901->13877->2789->20233->18803->27847.
All17valid same-vertex square sources (663 original divisors) have both channels
empty, although each has a unique simple nonresidue prime. Every source passes
the complete positive-factor mu_B characterization. Some failures are subgroup
coset obstructions, others finite-box obstructions. This is not closed under all
square edges: q20233,t3 supplies11, whose canonical source has both channels.
It is a negative control against equating the six-vertex factor-supply theorem
with coefficient positivity. No least-prime claim.

## Strongest exact target for Turn6

Use every first-half source, of either chi(a)sign. The existential version needs
only one p-coloured opposite pair; Mobius inversion is needed for exact counts,
not as an extra existence obstacle. The primitive Jacobi contributions at a
negative source are -1/phi(R) to E and+1/phi(R) to M; they cancel. Other characters
and the original factor-bound interactions remain. None of the pair formulas,
sharp restricted windows, inverse on the reciprocal source or finite counterexample
is a substitute for the required all-prime lower bound.

The notation C_p denotes E+M, not E+M/2. Preserve both integer counts separately.
