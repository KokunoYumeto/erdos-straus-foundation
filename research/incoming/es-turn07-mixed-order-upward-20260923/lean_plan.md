# Exact Lean-ready dependency plan — no build claimed

The plan is mathematical and version-neutral; no unchecked library identifier
or successful Lean execution is asserted.

1. OriginalState(p,a,u,channel): natural-number primality, p=1 mod4, p<4a<2p,
   u>0 and u|a^2. Define the exact gcd factors and prove h*r*s=a,h*r^2=u,gcd(r,s)=1.
2. Prove the E/M rational identities and their ordered rational inverses. Define
   Trace by the square gate and Full by the unsquared gate; derive equivalence
   with rational tail-sum integrality.
3. Define K,D from the factorization of R. Prove D|K,R=KD,R|K^2 and the additive
   isomorphism ZMod D -> the ideal generated by K in ZMod R. Prove the exact
   fine coordinate and its denominator-order formula.
4. Define original exponent boxes as a finite dependent product of integer
   intervals. Partition each by the order modulo K; prove disjointness, inverse
   and the one-word-per-coefficient formula over Z[ZMod D].
5. For steps lambda_i and strict chain g_j, prove that H_j=g_j Z/D and the class
   of lambda_ij generates H_j/H_(j-1), of order m_j. Construct the reverse digit
   recursion using the displayed modular inverses. Prove both compositions.
6. Enumerate full original radix blocks and unused coordinates. Show their
   returned words are distinct, proving the lower bound and the uniform equality
   case. Define the carried finite group law through the bijection; do not assert
   the componentwise product group law.
7. Prove no effective index can recur down a strict gcd chain. Induct over the
   finite divisor DAG to establish optimality of the rational-weight recursion
   in precisely the specified chain class.
8. Prove the actual-prime arithmetic realization, treating Dirichlet's theorem
   as a cited theorem with all CRT reducedness conditions discharged. For the
   sharp packet, prove the exact fine-defect function 1_{x notin H_m} and its
   stabilizer. No universal source existence axiom is allowed.
9. Define PlusCell(p,C,s) by s|(p+C), quotient=1 mod4,0<C-s<p. Prove the integer
   inverse and a'-a identity; add R'|(p+C) for the original unit-ray E return.
   Prove the full canonical trace-fibre inverse via every r|n,gcd(r,s)=1.
10. Verify the concrete p67369 source, all eight prefix shells, and the p97
    complete empty fibre by finite integer enumeration. Do not reuse the prior
    leastness over all primes as though this run independently proved it.
11. Formalize the full-order primality criterion: every prime divisor l of n
    has ord_l(b)=n-1, hence l>=n. Check complete factorizations recursively.
12. Derive the necessary counterfactual constraints, but leave universal source
    occupancy as the explicit unproved proposition. Stage7 has not closed it.
