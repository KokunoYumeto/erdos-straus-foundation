# Turn 3 handoff: a real route-selection gate

## What Turn 2 discharged

The full fixed-p nonresidue-prime graph is built without a favourable-factor hypothesis. All factors r of A_q=(p+sigma(q)q)/4 with (r/p)=-1 are edges; their exact valuations are retained. Composite 3q vertices and A_q>p/2 vertices remain. Both original E/M gates use the complete divisor box of A_q^2. Turn 1's stabilizer, fine coefficients and target collisions extend to this unit domain.

Four source/target parity types have explicit integer/CRT transitions. The prime r target is sigma(q)q=-p mod r. At modulus 3r the exact target is the pair (2,sigma(q)q mod r), or the full representative sigma(q)q-4cr; simply using sigma(q)q, or dividing by 3, is invalid. The common unchanged-divisor domain is exactly Div(gcd(A_q,A_r)^2), and even there a gate may cease to vanish.

The greatest fixed point U_infinity of failed vertices with all successors still failed is computed completely. q is outside it exactly when a path of at most |V_p|-1 edges reaches a genuine local hit. A failed sink SCC, not just a selected failed cycle, certifies a closed component.

## The proposed local closure is disproved

At p=2521 the actual sink cycle is
`11 -> 211 -> 683 -> 89 -> 17 -> 643 -> 113 -> 11`.
Every node has exactly its listed outgoing nonresidue prime, and both original selectors are empty. The complete graph has 187 vertices, four hit vertices, and 107 trapped vertices. The least nonresidue 11 is trapped. This is the least such hard prime by the certified prefix scan, not an ES counterexample.

At p=3361 the closed cycle
`31 -> 53 -> 11 -> 281 -> 1051 -> 1103 -> 31`
contains the genuine primitive sextic vertex 31. Its effective indices are 6,104,10,560,1050,1102. Increasing an index does not prove eventual escape or a well-founded decrease. The p=808369 source chain 43 -> 19 -> 28871 enters another fully certified failed sink cycle.

Do not spend Turn 3 trying to prove that every canonical closed component has a hit. That statement is false, including a component reached from a primitive sextic defect.

## What remains a possible global route

The much weaker universal statement U_infinity != V_p is equivalent to the existence of at least one hit in this canonical graph and is sufficient for ES. It is NOT proved. All 137 tested hard primes through 50,000 do have a graph hit; that census does not establish the universal statement. A graph with U_infinity=V_p would still only falsify this selected graph's coverage, not ES; the complete shell atlas must then be checked.

An alternative is a genuinely enlarged source. For a marked multiplier s with sq=3 mod4, sq<3p and (s/p)=1, A_(q,s)=(p+sq)/4 is an available source at the same p. At p=2521, q=11, s=5 yields A=644 and a real middle witness u=16. The old A=633 and new A=644 are coprime; no original-word transport from the old box was used. Do not infer that s=5, the two ports sigma and sigma+4, or all allowed ports work universally.

## Arithmetic data available for a deeper mechanism

For every edge retain p,q,r,sigma_q,sigma_r,m=v_r(A_q),B=A_q/r^m,c=A_q/r, complete target factorization, actual K, effective and ambient indices, phase, coefficient/cocycle data and original hits/misses.

A simple cycle's ordered coefficient word (sigma_i,c_i) has an exact inverse. With D=4^l product c_i, S=product sigma_i and cyclic affine numerators T_i, let h=gcd T_i. Then p=(D-S)/h and q_i=T_i/h. Every converse test, including closure and full gate failure, remains necessary. The existing failed cycles satisfy this equation; its affine contraction is not a contradiction.

For p=2521 the constants are D=8252375040, S=27, h=3273453. These are useful regression inputs for any proposed height, reciprocity or coefficient invariant. The inactive factor block from Turn 1 is not automatically preserved on an edge. There is no canonical group homomorphism between different moduli.

## Mandatory outcome for Turn 3

Attack either a constraint on ALL canonical components at once, or an explicitly justified additional arithmetic source/return. A local sextic lemma is useful only if it feeds that genuine global implication. If neither advances, pivot to the complete original-shell signed coefficient sum as specified in Turn -1. Preserve these closed examples as permanent negative controls. No density-to-pointwise step, generic positive Gram matrix, or supported-zero image can replace the needed arithmetic nonvanishing.
