## Additive mixed-support continuation

Add `research/incoming/es-mixed-support-20260914/` without editing previous work.
This follows the discrete-positivity predecessor and reads the pinned
Zeta JSA12–JSA20 proof at `d1191f9c2c47d4317c87262eca9e75c8de9ca7ff`.

The principal result is a ternary signed form on one original 9702-state hard
shell. Every two-column restriction is negative definite, but the full form has
inertia (1,2,0). An exact small rational trial yields `607462/1890375 > 0` and
reconstructs an original positive ES witness. The support functions are `1`,
`x*1_E`, `x*1_(beta_457>0)`, defined on one source with actual overlap. The two
arithmetic channels remain E and M.

The original metric, signed Schur coordinate inverse, bounded exponent tuples,
ordered denominators, all support intersections and split-fibre multiplicities
are explicit. The mixed factorial filter inequalities are valid uniformly in
arbitrary growing feature spaces in the actual Gram metric. An arithmetic
counterexample establishes that filtering a compressed operator instead would
give a false result; its exact relation-norm correction is retained.

This scalar form is adjacent to, but not identified with, PR #7's native
mixed-support cohomology.  The exact comparison is
`R^3 --A--> R^Omega --Pi--> H^0_arithmetic`, with `epsilon s A = Pi A`;
it lands in the
arithmetic successful-state `H^0`, not in PR #7's `H^-1` local-gluing class.

The previous hard prime and its eight-node complete-order certificate are
attributed and newly replayed, not claimed as a new prime. Standard-library
normal and optimized verifications cover the hard source, an independent finite
residue multiplication, the exact 44550-occurrence split, finite matrix/filter
fixtures and all 9531 small-shell tests. No new prime range, universal ES/RH,
Lean build, independent review or historical-priority claim is asserted.

Sources, all nine exact certificates, and readable PDF/PNG previews are included.
This additive draft records no merge and makes no change outside its new path.
