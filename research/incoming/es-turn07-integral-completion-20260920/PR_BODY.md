## Scope

Additive Turn 7 research continuation. No universal ES or Type-II occupancy
claim. The supplied literal-root signed rank-eight completion is specialized
at original arithmetic sources, retaining the original integral lattice.

## Main results

- Normalization indices p^2 (E) and p^9 (M), exact conductors, all invariant
  factors and the original T/eta action on the finite defect modules.
- The added E boundary point has p lifts modulo p^2 and none modulo p^3.
- Literal local rational-point counts 0/4 for E and 4/8 for M; these are not
  the preceding seven-point reciprocal cubic fibre.
- Exact integral obstruction to prime-only deck flip, labelled E/M interpolation,
  and the quadratic twist for reciprocal roots.
- For every fixed finite set of primes, a joint numerical E/M candidate can
  satisfy both gates, numerical bounds, the original-prime local data and
  specified finite integrality tests while failing the actual divisor box at
  one prime occurrence. Those primes have separately exhibited genuine ES states.

All proof coordinates and inverse domains are explicit. The local conductor is
not the Zeta programme's complex Gamma-weighted conductor.

## Checks

The standard-library main checker and separately implemented checker cover every
original first-half E/M state for all p=1 mod12 primes through3000. Exact matrices,
source labels and rational counter-controls are recorded. Ordinary and optimized
outputs agree on all mathematical tables. The local release includes a fresh
extraction replay and rendered PDFs. Finite checks are not an independent
mathematical review or an ES theorem.

## Provenance

Preserves the attached literal-root completion, the previous capacity theorem,
Elsholtz--Tao Type I/II coordinates, and standard local field/Dirichlet inputs at
exact locators. No historical-priority claim. No existing file is modified.
