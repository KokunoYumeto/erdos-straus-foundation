# Integral-completion integration audit

## Exact relationship to the preceding local bridge

The predecessor reciprocal-cubic packet and this literal-root packet are not identified.  The present order is

\[
\widehat{\mathcal S}=\mathbb Z_p[T,\eta]/(H,\eta^2-H')
\]

in the original ordered basis (1,T,T^2,T^3,\eta,\eta T,\eta T^2,\eta T^3).  The reciprocal chart has its own root order and quadratic twist.  `MORPHISMS.md` records the exact interpolation matrices, inverse domains, denominators, kernels and information loss.

## Normalization audit

- Exterior derivative valuations are ((1,0,0,1)); middle valuations are ((2,0,2,2)).
- The exact normalization matrix is the block matrix built from the original Vandermonde matrix and the diagonal powers (p^{k_i}).
- Exterior Smith exponents are ((0,0,0,0,0,0,1,1)), hence quotient ((\mathbb Z_p/p)^2) and index (p^2).
- Middle Smith exponents are ((0,0,0,1,1,2,2,3)), hence quotient ((\mathbb Z_p/p)^2\oplus(\mathbb Z_p/p^2)^2\oplus\mathbb Z_p/p^3) and index (p^9).
- The conductor ideals, multiplication actions, reduction kernels and nilpotent indices are computed in the same original bases.

## Fresh verification

The normal and optimized main runs agree:

```json
{"success": true, "checks": 104538, "primes": 99, "original_vectors": 868359, "E": 2451, "M": 2054}
```

The independent run imports neither the main verifier nor predecessor software:

```json
{"success": true, "checks": 27154, "primes": 99, "original_vectors": 868359, "E": 2451, "M": 2054}
```

The exterior local point counts split into 1,173 zero-point and 1,278 four-point fibres.  The middle counts split into 1,658 four-point and 396 eight-point fibres.  These are bounded verification inputs, not a new overall ES range.

## Figure and PDF QA

`figures/integral-normalization-smith.pdf` is generated by `tools/generate_20260920_illustrations.py`.  It was inserted into both readers, rebuilt, rendered and visually inspected.  The current workbench, preprint and CRT-control PDFs compile; the bibliography retains harmless underfull lines caused by long source identifiers and has no overflow.

## Boundaries retained

- The Type-I boundary point is added by finite completion; it is not an original square-divisor word.
- The joint-channel CRT family isolates an insufficient finite certificate; it does not refute the conjecture.
- A reciprocal coordinate change carries the displayed quadratic twist and is never treated as an untwisted equality.
- The analytic Gamma conductor is not identified with a finite integral conductor ideal.
