# The fixed-target capacity problem is complete; universal ES occupancy is not

Do not label this Turn 8. The ten-turn mnemonic's universal existence requirement
is still missing. This continuation finishes an exact unbounded subproblem and
states its limitation on original prime sources.

Proved:
- All W|j^2 with W=t mod(4j-1)>t are canonical, an unbounded companion, or the
  explicitly bounded finite templates ell< t/4.
- Every finite template has h<=t^2-3t+1, with exact equality conditions.
- If the canonical word is absent, no larger same-grade search can repair it
  above that bound. This is a theorem, not a scan heuristic.
- The bound is attained by original E states at infinitely many hard primes
  for each t=6 mod420; Dirichlet is applied to a reduced linear progression.
- Entire alpha lines t=1,2,3 and beta lines t|36 have forced original budgets.
- Every fixed alpha coefficient t>=4 has infinitely many actual hard-prime
  E states for which the complete same-grade middle box is empty.
- The full inverse-source domain includes the original E gate. The predecessor
  executable had it; its prose description omitted it.

The next step must not be “search more divisors at that grade”: the completed
classification tells exactly when that cannot work. A successful next arrow
must change the cofactor/source in a proved way, or force a positive coefficient
in the complete original first-half pair source at arbitrary p. Initial source
availability is still not supplied by a formal inverse or the Fabel fibre.

Permanent controls:
- t6,j5,h19: canonical6 absent, finite25 present and sharp; p825241 realizes it.
- t289,j1224: canonical289 and finite5184 coexist; count2 is not one support point.
- t1,j1000: companion4000 is unbounded, so finite bounds cannot be applied to it.
- p7840561,h43,t4: a genuine E state but whole grade43 middle source is empty.
- beta t8 at p346201, t27 at p593401, t5 at p2727841: all old-grade words fail.
- p3152041 has both negative-four-square coordinates but no old-grade M word.
- p5209,s5,r4 in the formal alpha factorization is not an E state; retain the gate.

The geometric prime-local ramification distinction from the predecessor remains
valid for actual returned states. This tranche does not need to redefine its
conductor or identify its supported zero with an absent source.
