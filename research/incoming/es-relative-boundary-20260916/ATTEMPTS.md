# Actual attempt record

## Aim
Continue the original factor-availability approach and the actual SplitZero coefficient comparison. The preceding global-chart packet needed many directions that were irrelevant after the target coset had already been selected.

## Attempt 1: raw-prime relative strides
Decompose raw original prime exponents by their order in a smaller dyadic quotient, then try to fill the remaining principal-unit subgroup. On all26 occupied branches missed by the preceding union, this adds no certificate. The exact exploratory record is retained as `attempts.json`. This is a bounded negative result for that search class, not a theorem that relative lifting is useless.

## Attempt 2: retain the actual affine exchange first
Keep the preceding disjoint singleton/pair lines and their compensating constants, then perform the disjoint lower/high stride decomposition. The proper-subgroup kernel map gives the correct return. This adds two branches and one prime against the stated previous union in the2m census.

## Evaluation repair: use the stronger baseline
A direct three-occurrence search in both roles, together with the previous tests, already covers all occupied dyadic branches in that census. The new method therefore has no coverage gain against that stronger union there. This is an evaluation correction, not a refutation of either sufficient theorem.

## Attempt 3: prove a structural separation instead of claiming an inflated gain
The actual high factor17 gives a family with precisely two cofactors, whose lengths tend to infinity. The previous full-chart canonical tests fail from their second prefix, while the proper subgroup is completely filled. A17-digit prime member was found with exploratory integer factorization software and then supplied with a standalone deterministic complete-order primality proof. The published verifiers need no symbolic-math package and do not import the prime-search software.

## Scope reached
The relative kernel, all quotient maps, actual exponent inverse, sharp one-prime capacity, complete counts and unbounded separation are proved. No condition asserting that a required target lifts is inserted without the explicit available high-capacity calculation.

## Remaining pointwise obstruction
For a fixed prime, different seeds could still have no lower target, or have lower targets only in fibres whose higher-capacity prefixes fail. The new maps expose that alternative; they do not show that the correlated seed values cannot all satisfy it. The target remains an arithmetic relation across actual factorizations, not a formal property of an arbitrarily supplied failure array.
