# Byte-preserved software dependency

`factor_exchange.py` is the exact supplied `/mnt/data/es_factor_exchange/verify.py`, SHA-256 `7b19ae5af165a5868ed3041cab9a35cf03701d783c8754876e2c2a4e5c5fef3f`. The new verifier explicitly imports it for existing arithmetic and baseline maps. The separate checker imports neither code file. Prior credit is The Clankers, with the source-specific references in the earlier package. No claim is made that repeated invocations of these checks are new independent verification.
