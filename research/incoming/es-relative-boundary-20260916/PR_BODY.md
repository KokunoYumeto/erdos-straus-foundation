## Proper-subgroup boundary lifting with exact ES certificates

This draft adds a standalone 21-file source tranche at `research/incoming/es-relative-boundary-20260916/`. No inherited file, `main`, or other pull-request branch is modified.

The earlier affine packet covered an entire cyclic chart. This contribution uses a proper principal-unit subgroup with an actual lower-digit target. The BR1 kernel isomorphism is applied literally; the ordinary quotient can vanish while the boundary kernel and original integer coefficients remain. All maps, role fibres and compensating prime exponents are supplied.

The complete source proves the disjoint mixed-radix lift, exact high-capacity prefix criterion, sharp one-prime count, all-exponent generating series and a bounded coarse-word return. The unbounded family has exactly two target cofactors with minimum occurrence length `2^(k-5)+1`; the old canonical full-chart and `(3,11)` tests fail. A separately proved prime member needs five/six factors in the two roles.

The complete scan through two million has a deliberately fair interpretation: the new union gains two branches and one prime against the preceding two-occurrence union, but **no coverage** against a direct three-occurrence search joined to the earlier tests. This is not presented as an overall coverage gain. The structural separation is a separate theorem.

Proofs, three-page preprint source, posting-card source, the verifier, a byte-preserved antecedent dependency, a separately implemented checker, exact source ledger, typed maps, complete certificate JSON and formalization plan are included. Fresh normal, optimized and separate-checker replays agree semantically with every supplied certificate. No universal ES theorem, source-wide analytic identification, new Lean build, independent mathematical review, or historical-priority claim is made.
