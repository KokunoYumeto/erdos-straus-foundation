# Research handoff — ES–Fable–RH multi-track continuation

## Verified starting point

Use the two commits in `SOURCE_LEDGER.md`. The five proof notes contain the complete derivations, not only task outlines. Run `python checks/run_validation.py` before integrating results. Preserve the source packet and this packet as separate additive generations. Do not modify sealed original files or replace prior proof status with the present finite checks.

No complete raw GitHub clone, native moment values, Lean build or repository write is included. The one uploaded WCF provider is byte-verified. Python/SymPy exact checks in this package are executable and their results were obtained in the current session.

## Track A — Arithmetic intersection of the extra frame divisor

Object: `N = S^6 F(-1/S,-e2/S,e3/S,-e4/S)`, an integer for distinct positive integral literal roots. The positive-real ES family in Track I has an exact irrational singular parameter, so positivity and the reciprocal relation alone cannot prove `N != 0`.

Next substantial target: determine whether `N=0` is compatible with the *original integral* hard-prime witness conditions, including channel gates. A successful nonvanishing theorem receives the explicit `||O^{-1}|| <= 41^3 S^12/4` coefficient bound. A valid integral zero requires a full original witness and exact rank/minimum audit; do not substitute the algebraic real slice for one.

Preserve the quartic coefficient hypersurface and full denominator marking. Do not infer universal ES occupancy from a frame bound or a fixed-prime finite search.

## Track B — Boundary duality and actual action

Object: `S_hat = R[r,eta]/(h,eta^2-h')` over `A != 0`, with residue functional `Lambda(f+eta*g)=[r^3] rem_h(g)/A`. The trace pairing factors through `M_(2 eta^3)`; that map, not residue duality itself, loses rank.

Next substantial target: derive the induced pairing/operator behaviour for the programme's chosen actual observation, including the full target minimum. Use the double-copy original conductor maps in Track II Section 7. For local ES/RH comparison, retain the unit
`c^3*(1+(2/(3p)+s*i/gamma)*epsilon)`, not just its scalar factor. A Hermitian comparison requires its own full Gram congruence and residuals.

The algebra has nonreduced fibres and the original physical coordinate has a pole. No assertion that this finite extension removes the original norm divergence, proves purity, or is an algebraic normalization is permitted.

## Track C — Evaluate the native residue-to-Gram multiplier

Object: `H = J_p q_(n-1)(C_p)` for the *actual native monic orthogonal polynomial*, with `q` retaining the original mass. For a different root polynomial, use `H = J_h k(C_h) + D` with every relation-defect entry in Track III.

Next substantial target: certified evaluation of the actual moments determining `p`, `q`, `k`, and `D`, then their return to the original remainder/observation/conductor fibres. ES nodes are not automatically native Gauss nodes. Do not delete `D` because both constructions have the same finite dimension.

## Track D — Propagate the improved inverse margin

Replace only the source interval floor `g_t` by `1/tau_t`, where `tau_t` is the explicit integer inverse trace. Preserve all density constants, masses, weighted moment shifts, native upper caps and quotient singular values. This can be propagated into each finite tolerance in the latest moment-to-current composition.

Next substantial target: an actual precision budget at a specified original degree, including the outer tail and the original source constants, and a verified calculation of the corresponding native moments. The degree floor improvement does not imply low overall computational complexity or a sign margin.

## Track E — Simultaneous parameter certificate

Use one common real diagonal `Theta` for all retained native resolvent rays. Do not invent independent data after the remainder and observation minima. The exact quotient matrix is
`G0 + Z Theta (I+K Theta)^(-1) Z*`;
its determinant ratio is `det(I+L Theta)/det(I+K Theta)`.

For fixed actual `W=[b_N,b_(N+1),v_lambda]`, the observed current numerator has degree at most two in each parameter and kernel/mixed numerators at most four. Both mixed products remain. Apply `checks/bernstein.py` to exact rational coefficient boxes only after proving the source box is positive definite and correctly contains the native point.

When native monic columns move, substitute Track V Section 6 before assigning polynomial degrees or normalization factors. A negative point of an enclosing box is not necessarily feasible for the actual correlated moment sequence. The outer infinite tail still needs the original TR/RR and current PD bounds.

## Closing proposition remains separate

Select the exact ES occupancy theorem or RH same-class sign/comparison theorem whose closure is sought. Identify how each new result contributes to a hypothesis of that proposition. Finite reconstruction, a perfect bilinear residue pairing, an invertible frame, or a successful synthetic polynomial certificate does not supply that closing theorem by itself.
