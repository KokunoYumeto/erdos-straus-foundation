# Attempt ledger: Turn 4

## A. Extend the surviving square-source supply mechanism
The reason for this attempt was the Turn 3 failure of constant sextic canonical closure. Each square source still supplies a real nonresidue factor below the same p. Counting the exact quadratic roots of old vertex primes gives a deterministic source that avoids a proposed small vertex set. The involution j -> -1-j improves the elementary inclusion-exclusion discrepancy to (3^n-1)/2 for a prefix. This produces explicit all-cardinality constants and a same-p finite reduction.

## B. Do not copy the all-3-mod-4 edge bound into the mixed graph
The exact root eligibility table allows opposite edges for four unordered mod-12 type pairs. An actual canonical closed pair at p=12889, vertices43 and61, was found. Every one of its22 valid same-vertex square sources has both local selectors empty. Thus the stronger immediate-occupancy proposal fails. The actual square-factor edges still leave the pair, and a checked path reaches a different occupied source.

## C. Universal finite cofactor reduction
A closed set's vertices must be at least p/H. Combining this with a canonical cycle and a least cofactor gives an explicit finite coefficient box. An exact monotonicity identity prunes that box. The finalized m=5 run enumerates all5,922,259 range-compatible profiles; the independent checker uses a different recurrence and binary search. Its three surviving cycles each have an actual square-nine escape. Thus every set of at most five has a valid exit within five square ports.

The m<=4 domains were additionally run without pruning in the separate checker. Enumeration over bounded coefficient words is not a prime-bound census; the proof first shows that every hypothetical small closed set appears in that finite domain.

## D. Preserve prime-power coefficient information
A base hit modulo R0 is encoded by an exact integer carry. Its digit modulo t^2 is the full gate, even when gcd(R0,t)>1. The p1129,q11,t11,u1845 example demonstrates that separate tests modulo11 and121 lose the1331 condition. The common original divisor span between square sources is calculated, not assumed to contain a transferable successful word.

## E. Exploratory graph scan
`discovery/probe.py` first completed a prefix through100000. A subsequent run targets1000000 and, only where the canonical graph has a failed closed set, introduces the first three square ports. It is discovery software, not the finite proof of the new theorem. Its completion or incomplete status is recorded separately from the finalized all-square scan through5000. No partly completed larger scan is treated as a completed result.

## Decision
Continue with original E/M carry interaction on larger/mixed source systems. The present proof supplies bounded factor expansion at arbitrary p and a finite reduction for fixed cardinality. It does not force a target coefficient on every reachable component. Higher-index geometry has not been assumed to disappear merely because a new factor occurs.

## Final proof transcription check
Before publication, the largest-vertex argument was corrected to write `4 v Q <= p + sigma q_prev < p + 3 Q`. The incoming cofactor can exceed the minimum v; equality was an unjustified transcription, not used by either exact checker. Both documents state the resulting bound unchanged.

## Completed exploratory record
The larger discovery run completed at bound 1,000,000: 2370 hard primes; 724 canonical-trap records; none retained a trap after the first three valid square ports were added. The source, raw record and completion log are sealed separately. This was not independently replayed at that bound and is not an input to any universal theorem.
