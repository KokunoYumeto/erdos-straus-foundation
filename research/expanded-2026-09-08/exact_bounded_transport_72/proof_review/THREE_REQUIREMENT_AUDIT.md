# Independent audit of the three requirements

The audit retains the complete prior reading and review provenance for the original arithmetic, cyclic/prime-incidence repairs, both sieve/raw-scale continuations, the repaired ordered no-hit map and the first eleven NS operator statements. This current record author independently read both complete frozen reverse proof files and the complete four-statement boundary proof without making mathematical source edits, checking every typed map, constant, analytical bridge, kernel and complete fibre. Earlier accepted unchanged calculations are carried forward with exact hashes; the historical main checker input is proved byte-identical after removal of only four explicitly named integration lines. Authorship, repair and separate independent reviewer scopes remain explicit. The exact current cumulative scope is fifteen files,72 named statements and85 transformations. A quotient is not treated as information loss when retained original data and full fibres reconstruct every original input. PDF and publication receipts are separate.

## Verdict

Final status: **passed after repair**, with **zero unresolved findings** in the current sources. The prior 47-statement pass missed the actual orientation and numerator errors recorded as PR-006/007; it must not be read as a historical all-three pass. Both those corrections and the earlier two missing bridges remain recorded below.

- **R1**: The current source retains original coordinates, constants, labels, bounds and complete fibres. PR-006 records the actual previously omitted exterior orientation and its complete repaired inverse; the old pass must not be used as evidence that this omission was absent.
- **R2**: The current audited calculations have no unresolved defect found. PR-007 records the previously incorrect pCD numerator and its exact correction to pC; PR-006 records the false prior completeness claim and all three corrected p-adic branches.
- **R3**: The two initial missing typed bridges, the repaired full ordered residual correspondence, and the new continuous/finite character distinctions have explicit coordinate maps, complete images/fibres, inverse branches and original arithmetic returns. The earlier61 transformations are retained; earlier missed defects remain recorded. The current85 transformations also prove every reverse transfer/deck fibre, common/absolute source conversion, physical/mean joint fibre, sharp spectral and analytical map, four raw candidate operations, rational obstruction classes, orbit-minimum fibres and exact occupied-R3 global first code. PR-008 through PR-011 preserve the new review repairs.

## Exact source versions

- `bounded_transport.tex`: `4356f53962f2da9b418e066f2c2ca1ba9572e45dc398bb2ca4d37981301e5050`.
- `connes_reading/connes_primitive_intersections.tex`: `77023419fd8d1dba895c7b4f765b294d86caacd748f13e6c16be4a07c211ea8c`.
- `exact_audit/fixed_y_successor.tex`: `181e6b50dab2adb1b33de0cc9df0dafda97cb84ab37b5409fd778d41022c050f`.
- `unary_boolean_transport.tex`: `6ee796fbc0a9ac286f9372e8f7345728dd8f785913dd79211e422ebdcf55af57`.
- `shared_variable_crt.tex`: `5637c0026d294db133ba4c2d16eadd6c15d3a1f7fcf60c15e2d59f7b1b284c6b`.
- `integral_cyclic_bridge.tex`: `49d4f511f86e5ca9429b0fb44b916ed0684241cd0f31181737bb04f29cdd1d85`.
- `input_output_incidence.tex`: `e7b09ec144f78f7507cdf87df2022fbb8651e5f7d491a5f7895a000b21fe4861`.
- `first_two_shell_sieve.tex`: `55b0653488fca8cd60c78b0d702d637a81f982e5f3ca797155777633fec96f7e`.
- `raw_scale_hit_incidence.tex`: `fbc86a3f127448b797d51bb99441c209340278e1bcf942135c77b6ba49306442`.
- `counterexample_sieve/all_shell_no_hit.tex`: `f440b496cf8298657b4bb17d2d97a12ce3392ca14ad09cb20859e97edbb608f2`.
- `ns_operator_bridge/torus_cover_lemma.tex`: `4f4fae9611f87d10896b6019342370d11f9d35e8f9a5a90bde844cd7dfd66704`.
- `ns_operator_bridge/sieve_character_cover.tex`: `916d7bc2a16d681c223de5f73330378f1babf70f7f4c0d657a93926deb5aae3c`.
- `ns_operator_bridge/reverse_proof_transport.tex`: `cf42ea9475aecd08268d1af5795fcbec92ca62b67d3ec0f73ddb04e187c74d62`.
- `ns_operator_bridge/reverse_smooth_inverse.tex`: `b9083db518d98550f2bd9928a0fbbc061d3a70ee846441774ce331be5288ad5b`.
- `boundary_swap/boundary_swap_completion.tex`: `aa2af846808a077ce951d279da4249df742ab18c4e0d0f1880a47c3a675c60dc`.

## Genuine gaps identified under the stricter requirement

### TR-001

Initial evidence: `bounded_transport.tex:372` at SHA256 `19829c321ef69d3dfddf05b98d099ae919826cd541818d544e4b8f5c43b7245f`.

The additive gluing complex was expressly distinguished from cyclic cohomology, but the five main proof inputs gave no typed map from that lattice complex into cyclic homology/cohomology data. This did not invalidate the lattice theorem; it failed the stronger requested composition rule.

For each finite abelian lattice quotientG, A_G=Z[G], I_G=augmentation kernel; proveG≅I_G/I_G² naturally via g↦[g]−[0], realize the lattice maps functorially, and pair with degree-zero traces toQ/Z that kill[0] andI². Prove characters separate every nonzero obstruction and retain the actual B-intersected lift fibre.

Status: resolved_after_independent_review.

Repair: `integral_cyclic_bridge.tex`, labels `lem:integral-augmentation`, `thm:cyclic-character-bridge`, `thm:cyclic-lattice-gluing`.

Read the complete integrated repair and checked every map, inverse, kernel, character-extension choice, naturality square and original box return. The integral quotient and Q/Z trace subgroup preserve finite torsion. Independent Smith-form and dual-character tests pass.

### TR-002

Initial evidence: `shared_variable_crt.tex:177` at SHA256 `e944046560f152f76a24435052ea4e63153dbaaa8ec14084a72c49dd36e9750b`.

The text distinguished integer output CRT classes from prime divisors of a chosen cyclotomic value, but did not type and calculate the surviving prime-divisor incidence between the two.

RetainI={(X,q):X satisfies original input class/bounds,q prime,q|Phi12(X),q in the prescribed outputclass}; compute the X-projection by exact divisor testing and the q-projection as the disjoint root-residue CRT fibres with full bounds. AdjoinDq and original positive denominator formulas as graph coordinates with exact inverse, without asserting any universal coverage.

Status: resolved_after_independent_review.

Repair: `input_output_incidence.tex`, labels `thm:input-output-incidence`.

Read the complete integrated incidence and final q73 fixture. Both finite projections, root/CRT kernels and interval fibres, inverse branch parameters, counts, graph return and full target shell inequalities are proved. Independent complete-factor/root incidence tests and positive witness checks pass.

Exact obstruction fixture: `{"p": 13, "A": 1, "B": 2, "C": 1, "epsilon": 1, "Dp": 2, "m": 8, "output_class": "13mod24", "F": 6227020800, "M": 37362124800, "X": 635156121600, "q": 73, "X_mod_q": 3, "Phi12_3": 73, "Dq": "19/2"}`. This refutes return of the selected divisor73 only; it does not exclude another passing prime factor.

## Continuation domain and orientation repairs

### PR-003 — thm:raw-scale-fibre

The initial raw-domain phrase called the entire tuple (A,B,C,D,epsilon) positive integer while also requiring epsilon in{0,1}. Literal positivity would remove the exterior tag0.

The actual p13 tuple(2,1,5,2,0) satisfies80=2+13+65 and a4 in the original range but has epsilon0.

The parent explicitly defined A,B,C,D in Z>0 and epsilon in{0,1}. Both original channels now belong to the typed domain exactly as used by the proof and fixtures.

Status: resolved_in_reviewed_source.

### PR-004 — thm:raw-first-half-code

The initial chi_H definition and generic radix statement referred to fixed seed coordinates, leaving their variable scope on an arbitrary decoded code implicit.

At p13 the fixed seed(2,1,5,2,0) passes, but the PCT code0 decodes(A,j,B,epsilon)=(1,0,1,0) and fails3|14. Fixed seed tests cannot define the total code bit.

The parent explicitly defined R(c)=4j(c)+3,a(c)=(q+R(c))/4, all three tests on A(c),B(c),epsilon(c), and the generic full-to-PCT map using its own decoded first denominator and four digits. The total indicator, least-hit selector, sentinels, layer bounds and radix correspondence now share the same fully defined code domain.

Status: resolved_in_reviewed_source.

### PR-005 — thm:raw-scale-fibre

The parent identified that the raw ray source named d=Rz-S whereas the main original reconstruction names d=Ry-S. These orientations were initially assigned the same unqualified name.

At p13, a4,y20,z130, the original d_y is8 while d_z is338; these are distinct and multiply to52².

The parent retained d_y,d_z separately, identified original d=d_y and source(d,e)=(d_z,d_y), and proved the complement maps with productS² and the coordinate-swap involution. The exact typed exchange preserves both original ordered triples and both source conventions with singleton fibres.

Status: resolved_in_reviewed_source.

### PR-006 — thm:shellwise-no-hit

The prior source claimed a unique d=p^epsilon*u representation with epsilon in {0,1} for every ordered witness. Original d_y has p-adic exponent 0, 1, or 2. The unswapped exterior chart therefore omits its opposite orientation. The prior 47-statement pass failed to detect this mathematical error.

At p=13,a=4, (4,130,20) has d_y=338=13^2*2, and (4,468,18) has d_y=1352=13^2*8. Both passing ordered witnesses were omitted by the unswapped exterior chart.

The source now retains exterior sigma in {0,1}; it gives the full inverse at each i=v_p(d_y): (0,a^2/d_y,0), (1,pa^2/d_y,0), (0,d_y/p^2,1). It proves both inverse compositions, every gate equivalence, the exact middle-complement swap, and all order-forgetting fibres. The all-shell emptiness criterion survives; the full ordered correspondence is now actually proved. The repaired checker independently enumerates every original S^2 divisor and compares the complete ordered image, detecting precisely the branch the old review missed. Historical reports remain preserved, not silently replaced.

Status: resolved_in_reviewed_source.

### PR-007 — thm:shellwise-no-hit

The prior common-denominator identity printed (pABCD)/a=pCD although a=ABD forces the quotient pC. The prior pass accepted an incorrect displayed calculation.

At p=13,a=4,u=2, the exterior chart is (A,B,C,D)=(2,1,5,2). Then pABCD/a=65=pC whereas pCD=130.

The source explicitly computes pC+p^(1-epsilon)B+A=4ABCD from (4ABD-p)C=A+p^(1-epsilon)B with no suppressed factor. The intended 4/p reciprocal identity is now correct with its original denominators, constants and tags. PR-007 records an actual calculation repair separately from the orientation repair.

Status: resolved_in_reviewed_source.

### PR-008 — thm:ns-sharp-smooth-inverse

The incoming sharp inverse statement extended its coefficient argument to distributional solutions without the complete test-functional realization and uniqueness argument required for that type change.

This was a missing proof bridge, not a false numerical estimate: an array assertion alone does not define a continuous functional on the specified smooth test space or prove that zero coefficients force that functional to vanish.

The source now states the exact H^(s+q)_0 datum domain and complex-linear distribution convention, proves polynomial coefficient growth and an explicit C^M pairing bound, computes the differentiation sign, and proves uniqueness through convergence of smooth Fourier sums in every seminorm. The inverse conclusion now reaches actual distributions with its full equation and zero-mean uniqueness. The original analytical constants and Pell proof remain unchanged. The initial source is preserved in the 58-statement history.

Status: resolved_in_reviewed_source.

### PR-009 — prop:ns-inverse-parameters

The source update used one inverse symbol across the common chart and absolute auxiliary coordinate before proving the typed coordinate conversion and all of its scale factors.

For common character e_k(y), its absolute pullback has frequency (J^i)^T k and derivative multiplier T_g^i 2*pi*i(v_t dot k). Treating the two directional inverses as the identical coordinate operator omits the factor T_g^-i.

S17 now defines Y,y,P_i,D_v^Y,D_v^y,T_v^Y,T_v^y,Pi_Y and Pi_y; proves Haar, derivative and inverse covariance on every original character; and derives P_i Delta v=-Q^-1-h T_t^Y P_i E_theta^circ with its original minus sign. The desired and actual axial increments retain A1 and the slow derivatives. The stronger inverse estimate is now mapped exactly to the actual source mean-correction coordinates, without erasing the cutoff remainder or claiming the complete PDE theorem.

Status: resolved_in_reviewed_source.

### PR-010 — thm:ns-continuous-transfer

The initial transfer proof supplied the projection compositions and the complete P_m and L_m fibres, but did not expressly give every Q_m fibre despite claiming every operator fibre.

This was a completeness omission, not a counterexample to the compositions: arbitrary target h outside the fixed-point image has empty Q_m fibre, while a fixed target has an entire affine kernel coset.

The final theorem and proof explicitly establish ker Q_m=ker L_m and Q_m^-1(h)=h+ker L_m if Q_m h=h, with empty fibre otherwise, using P_m injectivity and subtraction. All three transfer operators now have the complete typed fibres promised by the statement. The read-only transfer reviewer requested and independently rechecked this addition.

Status: resolved_in_reviewed_source.

### PR-011 — thm:boundary-orbit-code

The drafting review required the exterior code-decrease sentence to state that the returned lower record is an integral hit only on the calculated shell gate. The rational candidate code itself is lower whenever A0>B0.

At p=373,R=7 the exterior records with (A,B)=(19,1) and (5,1) have lower swapped candidate codes, while their forced swapped coefficients 7088/7 and1866/7 have exact denominator7 and hence do not give integral hits.

The theorem now says that the same-tag swap gives a lower integral hit exactly on R|p^2-1; outside it the exact rational companion and its nontrivial obstruction remain. The proof retains the complete code-difference formula and the independent integrality table. The orbit-minimum conclusion now explicitly compares actual hits. The complete failed integral fibres and rational relation survive; no occupancy or new witness is inferred from a smaller arithmetic code alone.

Status: resolved_in_reviewed_source.

## Coordinate-level audit inventory

### R32 — Integral cyclic homology and lattice augmentation coordinates

Evidence: `integral_cyclic_bridge.tex:7` (`\label{lem:integral-augmentation}`).

Domain A_G=Z[G]=HC_0^Z(A_G), with its integral augmentation idealI_G; codomain finite or general abelianG throughI_G/I_G².

kappa_G(g)=(u_g-u_0)+I²; theta_G(sum n_g u_g+I²)=sum n_g g are inverse homomorphisms. FullHC0 coefficient-map kernel isZ*u_0+I², with every fibre explicitly stated. A group mapf inducesu_g→u_f(g) and commutes withkappa.

The ring isZ throughout; no rational coefficients erase torsion. FullHC0, its augmentation ideal, and its quotient are expressly distinguished and connected by typed maps with complete kernels/fibres and inverse. This repairs the initially unsupported complex/cyclic distinction.

### R33 — Degree-zero cyclic traces and exact torsion characters

Evidence: `integral_cyclic_bridge.tex:68` (`\label{thm:cyclic-character-bridge}`).

Domain Hom(G,Q/Z); codomain the stated subgroupT_G ofHC^0(A_G;Q/Z) withtau(u0)=0 andtau(I²)=0, rather than all cyclic traces.

chi maps to tau(sum n_g u_g)=sum n_g chi(g), inversechi(g)=tau(u_g-u0). The explicit finite-subgroup extension solvesm*beta=chi(mx) with allm values and proves exact restriction fibreschi0+Hom(G/H,Q/Z).

Every nonzero finite-orderg is detected by extendingg→1/order(g). Q/Z retains torsion. The maps have singleton fibres, and no trace subgroup is mistaken for the whole cohomology group.

### R34 — The actual exponent obstruction in cyclic coordinates

Evidence: `integral_cyclic_bridge.tex:143` (`\label{thm:cyclic-lattice-gluing}`).

Domain original finite exponent quotient exact sequenceG_L→G_0→O; codomain its integral augmentation quotient sequence and dual degree-zero trace subgroups.

Functorial kappa conjugatesi,delta with the displayed basis maps. Pullback traces give0→T_O→T_G0→T_GL→0 with proved kernel, full extension fibres and inverses. Every trace vanishes ono iff o0; original lifts are exactlye0+K_L andB intersect that coset.

The bridge connects the exact original lattice complex to cyclic data, calculates torsion obstruction faithfully, and explicitly returns to every original bounded exponent coordinate. Vanishing cyclic character evaluations still do not assert box occupancy.

### R35 — Cyclotomic inputs and prescribed prime-output witnesses

Evidence: `input_output_incidence.tex:67` (`\label{thm:input-output-incidence}`).

Domain finite labelled incidence(X,q) with original solved input class/bounds, primeq>p, full output residue filters andq|Phi12(X); codomain its two projections and full labelled witness graph.

Each fixed-q fibre is the disjoint union over all rootsrho ofPhi12modq satisfyinggcd(M,q)|(rho-a_in), with explicitCRT representative, kernelLqZ, all boundedt intervals and inverse(rho,t). Each fixedX fibre is its full filtered prime-divisor list. Dq and(aq,yq,zq) are appended by graph bijection, with the original input and seed retained.

The exact two-sided incidence repairs the initial output/input distinction. Prime labels are counted once, roots and all lifts remain, and original source shell plusq>p proves the complete target shell range and positive ordered witnesses. The q73 fixture calculates failure for one selected factor only, preserving the remaining finite correspondence.

### R01 — Positive denominator triple to primitive ratio

Evidence: `bounded_transport.tex:49` (`\label{lem:units}`).

Domain: ordered positive ES triples(p,a,y,z) with the original prime and allowed shell. Codomain: retained(p,a,S,R,m,n) with positive coprime m,n|S and R|(m+n).

d=Ry-S; (m,n)=red(d/S). For v_l(S)=b_l,v_l(d)=c_l, the exponents are(max(c_l-b_l,0),max(b_l-c_l,0)). Inverse d=Sm/n and y=(S/n)(m+n)/R,z=(S/m)(m+n)/R.

The discarded common factor is recovered from S and the reduced pair; S=pa and R=4a-p stay attached. Positivity, exclusive prime support and every exponent bound are proved. Complement swaps m,n and y,z. No scale or sign is lost.

### R02 — Finite signed box versus generated group

Evidence: `bounded_transport.tex:108` (`\label{eq:signed-box}`).

Domain: product of integer intervals[-v_l(N),v_l(N)] indexed by primes of the retained N. Codomain: positive reduced rational ratios of coprime divisors of N.

beta maps to product l^beta_l; inverse is the vector of rational prime valuations. Positive and negative parts reconstruct numerator and denominator.

The actual domain stays finite; no subgroup closure is substituted for it. The original N remains a parameter, so its prime support and exponent caps survive.

### R03 — Full pair system to E/M charts and unordered counting

Evidence: `bounded_transport.tex:118` (`For direct comparison with the two supplied channels`).

Domain: passing d|p²a² with retained ordered denominator orientation. Codomain: tagged p-exponent i0,1,2 and v|a², then E/M channel sets and complement orbits.

d=p^i v uniquely; i2 gives E via multiplication by4p inverse moduloR, i0 is its complement, and i1 gives M. Complement v maps to a²/v in M; a passing fixed point would force R|2 and is excluded.

Both exterior orientations and both middle orientations are counted with their correct multiplicities. The source sets and complement map remain present when cardinalities are taken; the count is not used to reconstruct a lost witness.

### R04 — Existential permutation and unary cutoff

Evidence: `bounded_transport.tex:150` (`\subsection{The full primewise biconditional`).

Domain: positive ordered ES triple with its retained coordinate permutation; full shell union and its restricted A<=2 sublocus.

Choose a least denominator by a retained coordinate permutation. p/4<a<=3p/4 gives the entire original interval. For u/a=B/A reduced, a=ABs,u=B²s with inverse A=a/gcd(a,u),B=u/gcd(a,u),s=gcd(a,u)²/u.

The permutation is retained to restore order. The cutoff map is the inclusion of a specified sublocus; the complete p37,a12 counterexample and surviving p37,a10 hit show precisely what its per-shell nonidentity does and does not establish.

### R05 — Local exponent quotients and exact obstruction

Evidence: `bounded_transport.tex:243` (`\label{thm:glue}`).

Domain Z^s with original finite box B retained. Quotients are Z^s/K_D,Z^s/K_E,Z^s/(K_D+K_E).

Diagonal i([e])=([e],[e]); delta([x],[y])=[x-y]. For x-y=k_D+k_E, e0=x-k_D=y+k_E. Complete integral fibre e0+(K_D intersect K_E); arithmetic fibre B intersect that coset.

Changing a representative changes only the proved kernel. Negative exponents use unit denominators, and K_lcm=K_D intersect K_E is proved. A vanishing group obstruction is never equated with occupied bounded fibre.

### R06 — Noncoprime residue compatibility

Evidence: `bounded_transport.tex:303` (`\label{prop:crt}`).

Domain Z/lcm(D,E)Z. Codomain the ring fibre product over Z/gcd(D,E)Z, and its unit restriction.

Reduction is the map. Its inverse is w=r+D*((D/g) inverse*(s-r)/g modulo E/g), with both gcd compatibility and reduced modulus-one case retained.

The residue map, exponent image and finite-box preimage are not collapsed. The ring inverse preserves exactly its congruence class; all integer or exponent lifts are separately recovered through their proved fibres.

### R07 — Finite residue-order reduction

Evidence: `bounded_transport.tex:337` (`These constructions are effective without`).

Domain full integral exponent lattice, with B still its arithmetic subset. Codomain product of remainder intervals0,...,ord_L(q_j)-1.

Coordinate Euclidean division e=f+(T_j z_j)_j; complete kernel is the union of every passing f plus product T_j Z. Targets use the same union with their exact residue test.

Choosing standard residues is reversible once the complete lattice fibre is retained. Bounds are intersected coordinate by coordinate, rather than inferred from image membership.

### R08 — Affine coefficient division, divisor bounds and monoid counting

Evidence: `bounded_transport.tex:515` (`\label{thm:divisor-crt}`).

Domain positive u satisfying all original upper-divisor, mandatory-factor and affine congruence constraints. Codomain exact prime-exponent choices in their computed residue class.

N=gcd(N_i),f=lcm(f_j) retain the intersection of budgets. Each congruence divides by g=gcd(c,M) only after g|b; reduced modulus M/g and residue c/g inverse*b/g are retained. Full residue monoid includes zero and nonunits, and stored labelled prime choices reconstruct u.

Zero coefficients and modulus one are explicit. Every original inequality and divisibility is recovered. Numeric interval1<=u<=N is not substituted for prime-exponent bounds. Coefficients count retained choices, which remain available as complete lifts.

### R09 — Rational ratio transport versus integral shell transport

Evidence: `bounded_transport.tex:694` (`\label{thm:ratio}`).

Domain positive rational shell at a, codomain positive rational shell at b, with fixed p. Integral restriction uses coprime m,n|p*gcd(a,b) and lcm(Ra,Rb)|(m+n).

T_ab(a,y,z)=(b,b*Ra/(a*Rb)*y,b*Ra/(a*Rb)*z), inverse T_ba, composition T_bc*T_ab=T_ac. Same primitive ratio determines both reconstructed integer pairs uniquely.

The rational scaling constant is retained exactly; it is never assumed integral. Prime specialization and shared exponent minima remain explicit. Adjacent restriction is calculated completely as the two pairs(p,1),(1,p) exactly when R(R+4)|p+1.

### R10 — Positive shears and arithmetic return domains

Evidence: `bounded_transport.tex:806` (`\label{thm:shear}`).

Domain passing primitive pair at a; codomain passing primitive pair at a+1. Matrices L and J are the stated positive unimodular shears.

L(m,n)=(m+n,n), inverse(x,y)=(x-y,y); J is conjugate by swapP. Exact L domain has n1,m|a,m+1|a+1 and both residual gates. Unique parameters m=R(R+5)/4-1+R(R+4)t, a=m+c*m(m+1) retain t,c>=0.

No normalization of unchanged coordinate is performed: n=1 is proved from primitivity, prime supply intersection and size. The original p and shell restrictions remain. Same-shell failure is proved for this specific map, and the strongest adjacent return is classified.

### R11 — Arithmetic successor graph versus arbitrary matrix words

Evidence: `bounded_transport.tex:894` (`\label{thm:no-two-shears}`).

Vertices are fully labelled(a,m,n) with actual passing witnesses; edges are adjacent L/J returns. Codomain is the next arithmetic vertex, not an ambient rational matrix orbit.

Every edge has its displayed inverse subtraction. A modular-three argument excludes repeated edges; mixed types fail their unchanged-coordinate requirement. Exact indegree/outdegree bounds prove a directed matching.

The distinction is supported by the classified domain inclusion into the ambient matrix action and a computed arithmetic obstruction. It does not declare other matrix words or fixed-y maps unrelated; their intermediate-domain requirement is retained explicitly.

### R12 — Norm composition, orientation and original endpoints

Evidence: `bounded_transport.tex:991` (`The assignment of the two norms`).

Domain retained ordered primitive endpoints(u,v),(x,y) with norms p,p+2; codomain decorated norm data including T=ux+2vy and signedDelta=uy-vx.

The full expansion proves T²+2Delta²=p(p+2). For cone orientation use(V0,W0)=(v0,w0) whenDelta>0 and the swapped order otherwise; original v0,w0 and their norm labels remain recorded.

Squaring Delta in the identity does not erase its separately retained sign. Neither endpoint is replaced by its norm or by inserted vectors. The orientation change is the explicitly recorded identity/swap map, whose inverse restores the retained labels.

### R13 — Decorated subdivision and coordinate bounds

Evidence: `bounded_transport.tex:1024` (`\label{lem:split}`).

Domain positive primitive oriented generatorsV,W with determinantD>1; codomain split data(r,E,Z) and child cones, retainingV,W.

W=rV+DE,det(V,E)=1,1<=r<D uniquely. Z=V+E=((D-r)V+W)/D. These are exact formulas and recoverE=Z-V, thenW=rV+DE.

The full original generators remain. Strict positivity, primitive inserted ray, determinant values1,D-r and original coordinate maxima are proved. The determinant decrease does not replace the original norm equations with changed endpoints.

### R14 — Marked returns, finite divisor test and denominator return

Evidence: `bounded_transport.tex:1078` (`\label{lem:marked-finite}`).

Domain full tagged tuple(q,k,t,b,e), determinantqt-pk1, markq+e=4bk and both columns in the cone. Codomain retained(e,t,k,delta) finite parameters, and a separately defined positive ES triple.

delta=(et+1)/k, delta=-p modulo4t; inverse b=(p+delta)/(4t),q=(pk+1)/t. Tagp reconstructs(bk,bt,pbkt), tag1 reconstructs(bt,pbk,pbkt).

All retained tuple coordinates are recovered exactly by the finite parameter inverse; tags remain even if the same matrix meets both marks. The denominator map is a proved map, not asserted to identify differently tagged matrices. Positivity and all divisions are checked.

### R15 — Parent cone versus canonical leaves and empty subcone

Evidence: `bounded_transport.tex:1125` (`\label{thm:orchard-partition}`).

Domain tagged arithmetic returns in the original norm cone; codomain disjoint union of returns in canonical leaf cones.

Map is unique leaf assignment, identity on(q,p,k,t,b,e); inverse is the inclusion of that leaf tuple. Strict straddling would give splitterc=Aq+Bp>p, contradicting the proved bound. The p1201 alternate empty cone is included by explicit positive rational endpoint coefficients.

The unique leaf map preserves the entire marked witness and all budget predicates. The separate empty cone is not dismissed as unrelated: its actual containment and complete empty-return test are supplied, while another canonical leaf contains the exhibited return.

### R16 — Finite divisor box and embedded ordered group

Evidence: `connes_reading/connes_primitive_intersections.tex:44` (`\label{prop:connes-finite-return}`).

Domain retained finite budgetN and its exact divisors; codomain specified embedded subgroupH_N=(1/N)Z ofQ.

Build H_N as union of(1/d)Z for d|N. Exact divisor return d>0,1/d∈H_N iff d|N. H_N intersect H_M=H_gcd and H_N+H_M=H_lcm with kernel(t,-t) for the sum map.

Passing to an embedded group does not discardN: it is recoverable from its least positive element, and reciprocal membership recovers every divisor budget. The sum and intersection maps have their full kernels and fibres.

### R17 — Embedded intersection and original witness pairs

Evidence: `connes_reading/connes_primitive_intersections.tex:107` (`\label{thm:connes-common-pairs}`).

Domain common primitive shell pairs; codomain primitive reciprocal pairs inH_p*gcd(a,b), marked by lcm residual divisibility.

Coordinatewise reciprocal map and inverse; k_t=(S_t/(mn))*((m+n)/R_t), denominators(k_t m,k_t n). Inverse k_t=gcd(denominators), then their primitive reduction.

The scale k_t is reconstructed rather than suppressed. The primep, shellt, residual and exponent minima stay attached; both orientations and singleton common-ratio fibres are proved.

### R18 — Abstract ordered-group isomorphism versus embedded budget

Evidence: `connes_reading/connes_primitive_intersections.tex:225` (`\label{prop:connes-abstract-loss}`).

Domain finite embedded H_N objects; codomain their abstract ordered-group isomorphism class. Individual map phi_NM:H_N→H_M.

phi_NM(x)=(N/M)x; all finiteN lie in one abstract-class fibre. On reciprocal divisors u=N/e maps toM/e exactly for e|gcd(N,M). On primitive shell labels m,n it multiplies both byb/a; integrality then primitivity forcea=b.

The abstract quotient is explicitly information losing, with its complete finite-object fibre stated; it is never used alone as a budget. The retained embedded intersection and subsequent shell-dependent divisor maps provide the strongest surviving exact arithmetic correspondences.

### R19 — Middle divisor chart and primitive pair chart

Evidence: `connes_reading/connes_primitive_intersections.tex:286` (`\label{prop:connes-middle-return}`).

Domain positive divisorsu|a²; codomain coprime m,n|a.

mu_a(u)=(u/g,a/g),g=gcd(u,a), inverseu=am/n. Per-prime max/min formulas preserve every exponent. u+a=g(m+n) withg aunitmodR identifies marks exactly.

Neither the gcd nor the scale is lost; it is recoverable from the retaineda. The exact ordered denominator formulas retain ratio u/a and all positive integer conditions.

### R20 — Finite-point isomorphism versus middle-endpoint shear

Evidence: `connes_reading/connes_primitive_intersections.tex:326` (`\label{thm:connes-marked-successor}`).

Domain unique reciprocal top-divisor returna²→(a+1)² for consecutive budgets, with its exact E/M marks.

The mu maps send endpoints to(a,1),(a+1,1), which are related byL. E marks are excluded by a proved quadratic-residue obstruction; both M marks hold exactly whenR(R+4)|p+4. Full parameters and original triples are explicit.

This different domain is not verbally conflated with the forbidden coordinatewise primitive map: the displayed commuting square gives the precise relationship. The prime and shell conditions are retained, with exact inverse on the marked image.

### R21 — Affine correction versus additive group morphism

Evidence: `bounded_transport.tex:1280` (`\label{prop:affine-point}`).

DomainH_N, codomainH_M with integer correctionc; restricted domain positive reciprocal divisors with retained indices.

Psi(x)=(N/M)x-c/M; inverse(M/N)y+c/N; zerofibre{c/N}; positive-image domainNx>c. Divisor returnu=N/e→M/(e-c) iff e|N,e>c,e-c|M. Composition adds correctionsc+d with intermediate budget still required.

The distinction from an additive homomorphism is proved byPsi(0), and the exact affine map has all fibres. No kernel is falsely assigned to a nonhomomorphism. Standardization of indices preserves all input budgets.

### R22 — Full L correction and exact J conjugation

Evidence: `bounded_transport.tex:1340` (`\label{thm:full-connes-shear}`).

Domain classifiedL edges and their exact middle divisorsu=am,v=(a+1)(m+1); J uses complemented source/target divisor loci.

e=a/m=1+c(m+1),eprime=(a+1)/(m+1)=1+cm, soPsi(1/u)=1/v. On reciprocal divisors iota_N(x)=1/(Nx); iota_M Psi iota_N(x)=x/(1-cx), with labelu→u-c and exact inverse/domain.

The c>0 obstruction for the uncorrected map is proved, not treated as unrelatedness. Complete L/J corrected returns retain original denominator order throughP. Complementation is not asserted as a self-map of every element ofH_N.

### R23 — Fixed original denominator and rational/primitive target

Evidence: `exact_audit/fixed_y_successor.tex:20` (`\label{thm:exact-audit-fixed-y}`).

Domain integer shell witness(y,z) withA+z|A²,A=a(a+1); codomain target integer shell(y,zprime) with0<zprime<A,A-zprime|A².

zprime=Az/(A+z), inverseAzprime/(A-zprime). Raw pairU=a(R+4)m+pn,V=(a+1)Rn andg=gcd(U,V) give the target primitive pair(U/g,V/g).

The original y is retained. Strict denominator decrease and exact factorization are proved. Gcd reduction keeps the raw U,V,g and has a reconstruction through the target shell; its full target budget is equivalent to the computed integer divisibility, not to mere rational existence.

### R24 — Unrestricted unary code versus tagged exponent boxes

Evidence: `unary_boolean_transport.tex:68` (`\label{thm:unary-box}`).

Domain disjoint union of full tagged valuation boxes(j,epsilon,e_l). Codomain precisely the budget-admissible code subset, not the whole rectangle.

u=product l^e_l; g=gcd(a,u),A=a/g,B=u/g,D=g²/u, then exact mixed-radix code. Inverse decode andu=B²D, D=a/(AB), then valuations. Passing gates reconstruct original chart triple(ABD,p^epsilon ACD,pBCD).

The singleton tag, allA,B<=9h and every exponent are retained. The source chart orientation(A,p^(1-epsilon)B) is proved and related explicitly byP to mu_a in the middle channel. RestrictedA<=2 remains an optional predicate.

### R25 — Truth-vector and cardinality projections

Evidence: `unary_boolean_transport.tex:167` (`\label{thm:unary-truth-fibres}`).

Domain full passing coded set; codomain bit vectors, Boolean-selected unions and associated counts.

rho(c) records all exact predicate bits; full fibresH_sigma and their box preimages are explicitly retained. Common-domain fibre products are diagonal forr>=1. Union projection fibres are exact membership tags; least tag gives a disjoint representation.

Truth aggregation is a noninjective map with complete fibres, not a replacement for a witness. Chi kills invalid candidates, all partial coordinates are guarded, and middle-invariance is retained when interpreting weighted counts as unordered counts.

### R26 — Boolean predicates versus positive affine CRT atoms

Evidence: `unary_boolean_transport.tex:280` (`\label{prop:unary-crt-fibre}`).

Domain one fixed(j,epsilon) component and one complete truth vector. Codomain candidate divisor list followed by a full exact test on the sameu.

Collect true fixed-coefficient affine/divisibility atoms with the original gate and bound; use the proved bounded CRT, reconstruct each code, then retain exactly its target truth vector.

False predicates and variable coefficients are never replaced by an invalid local CRT simplification. The algorithm proves both inclusions and complete integer lifts; negated composite divisibility remains globally aggregated.

### R27 — Affine input constraints and standard representatives

Evidence: `shared_variable_crt.tex:16` (`\label{prop:input-crt}`).

Domain all original integerX congruence constraints, including6F|X with every nonzero source exclusion retained.

Individual gcd reduction and pairwise-compatible residue merge yielda+MZ with kernelMZ; inverse parameter(X-a)/M. Positive and bounded intersections use exact floor/ceiling inequalities.

No original congruence or zero-coefficient solvability condition is discarded. Standard representative selection is accompanied by the full fibre and inverse parameter.

### R28 — Seed output integrality versus output residue class

Evidence: `shared_variable_crt.tex:97` (`\label{prop:output-crt}`).

Domain fixed positive integer seed(p,A,B,C,Dp,epsilon), codomain integerq withDq=(Omega+qTheta)/K0.

K0=g*m,Theta=g*w withgcd(w,m)1; Dq integer iff q=pmodm. Simultaneousq=1modD has explicit CRT representative and lcm fibre. Reduced denominator atq1 ism/gcd(m,p-1)>1.

The seed, both gcd factors and parameter scales remain attached. The proof preserves the distinction between integer outputs and selected prime factors; its missing explicit incidence at the initial audit is recorded separately.

### R29 — Signs, inequalities, quotients and remainders

Evidence: `shared_variable_crt.tex:218` (`\label{lem:canonical-auxiliary}`).

Domain a finite original integer box with retained admissibility and positive variable divisors. Codomain augmented box of original variables, one-hot signs, inactive-zero slacks and canonical quotient/remainder variables.

The polynomial sign residuals recover the exact sign and magnitude slacks. f=dq+r,d-1-r-s=0 with nonnegative r,s recoverq=floor(f/d),r=f-dq,s=d-1-r uniquely; all explicit bounds includeF0 andG1.

Original f,d,x remain present; sign is not replaced by an unrecorded absolute value. Projection forgets only uniquely recoverable auxiliaries. Boolean false equalities are encoded as bits, not imposed as true residuals.

### R30 — Polynomial residuals to vector CRT and back

Evidence: `shared_variable_crt.tex:322` (`\label{thm:shared-variable-crt}`).

Domain full proved augmented box, codomain compatible tuples of vector residues, retaining the chosenD_fixed andL=D_fixed(H+1).

KernelLZ^k; all liftsr+Lt have displayed coordinate floor/ceiling bounds and exact inverse t. Local residual zeros imply integer zeros because |f_a|<=H<L. Original admissibility and completePhi are then tested on each commonlift.

No coordinate injectivity is inferred from residual zero-detection: the three lifts of0mod2 remain. Distinct representatives, all variable budgets, negative signs and unique auxiliary inverse survive; no separately chosen local witness is substituted.

### R31 — Concrete ES polynomial truth presentation

Evidence: `shared_variable_crt.tex:473` (`\label{cor:actual-prime-truth-system}`).

Domain full(j,A,B,epsilon) rectangle, codomain the actual common divisibility/coprimality/gate truth system and its canonical reconstructed coordinates.

p+(1-p)epsilon is exactlyp^(1-epsilon) on retained bits. Coprimality is equivalent to excluding every commond≥2up to9h. Canonical quotientsD=a/(AB),C=T/R and code inverse recover all hits.

All divisors are positive. ExplicitD,C,u,y,z bounds are proved before applying the shared-variable construction. No unproved positivity is supplied by a selected bit; exact original denominator formulas establish it.

### R36 — Residual-three parity fibre and complete count

Evidence: `first_two_shell_sieve.tex:19` (`\label{thm:r3-factor-sieve}`).

Original prime-labelled exponent box with every e_ell in0,...,2v_ell; codomain both original channel fibres and the computed scalar count.

The map e→u is inverse to valuations. Both fibres impose odd sum on residue2 coordinates; the alternating product gives P1(P2-1)/2. Each residue2 factor gives the explicit A1 chart u=a*ell in both tags.

The scalar count is not used as reconstruction data: the complete original exponent fibre remains. The residue1 multiplierP1, all multiplicities, both tags and original first denominator survive.

### R37 — Residual-seven logarithms, budgets and factor predicates

Evidence: `first_two_shell_sieve.tex:96` (`\label{thm:r7-factor-sieve}`).

Original divisor exponent box to the finite log groupZ/6, retaining the actual prime factorization and all exponent caps.

The full log table gives fibres sumlambda*e=5 andb+3. Group-ring coefficients count exactly these original fibres. The disjunctive criterion has explicit budget-valid witnesses and an exhaustive converse.

Aggregate n3,n24 decide occupancy only; they never replace the original vectors, coefficients or exact inverse valuations. Division by the residue4 factor is integral with exponentb_t-1.

### R38 — Same-shell occupancy and the smaller unary layers

Evidence: `first_two_shell_sieve.tex:291` (`An occupied residual-seven shell outside the first two layers.`).

The complete p373,a95 tagged divisor fibre, and its subset with computedA<=2.

All nine exponent vectors give E={5,19},M empty; their exact A coordinates are19 and5 and the ordered triples are computed.

This calculates the precise nonempty full fibre and empty restricted fibre rather than inferring an unproved relation to other shells or global positivity.

### R39 — Two-shell rejection and the original tagged graph

Evidence: `first_two_shell_sieve.tex:215` (`\label{cor:two-shell-sieve-return}`).

The union of both retained original shell loci and its explicit integer-coordinate graph.

g=gcd(a,u),A=a/g,B=u/g,D=g²/u,C=(A+p^(1-epsilon)B)/R; inverse retainsp,a,epsilon and takes valuations ofu. The simultaneous rejection is exactly the conjunction of the two proved empty-fibre predicates.

All p,a,R,S, exponent bounds and ordering remain. Both shells lie in the first half, while any smaller A layer is tested on the calculated coordinate. No survivor infinitude or global emptiness is inferred.

### R40 — Raw tuples and square-divisor scale fibres

Evidence: `raw_scale_hit_incidence.tex:15` (`\label{thm:raw-scale-fibre}`).

Positive A,B,C,D with tag0or1 and original full shell, to a coprime seed together with its retained positive scalek.

k=gcd(A,B); (A0,B0,C0,D0)=(A/k,B/k,C/k,k²D). Inverse multipliesA0,B0,C0 byk and dividesD0 byk². Forgettingk has the full square-divisor fibre and count.

The original scale is recoverable; C divisibility is proved usingk<p and the raw equation. All labelled denominators and quotient invariants agree by exact substitution.

### R41 — Original oriented divisors and the ray source orientation

Evidence: `raw_scale_hit_incidence.tex:123` (`The ray source's ordered labels`).

Original ordered pair(d_y,d_z) and source pair(d,e)=(d_z,d_y), both on positive pairs of productS².

The exact swap(r,s)→(s,r) is its own inverse; d_z=S²/d_y and vice versa. Main originald remainsd_y.

Both orientations and original scaleS survive. The repaired source name collision is closed by a complete typed involution, not merely a warning that the names differ.

### R42 — Primitive ray and fixed raw-scale return

Evidence: `raw_scale_hit_incidence.tex:131` (`\label{thm:raw-scale-period}`).

Signed integerq with the retained primitive and raw rational D coefficients, restricted to positive returned primes only where stated.

D0(q)=k²Dk(q); exact periods m0,mk satisfy mk/m0=k²/gcd(k²,w). At a common return the full scale fibre is k²|gcd(D0p,D0q), with inverse division byk².

The primitive ray does not enlarge a fixed raw-scale return domain. Signedq integrality, positivity atq>=p, all constants and original full shell bounds are proved separately and preserved.

### R43 — First-half gate and exact PCT/full radix recoding

Evidence: `raw_scale_hit_incidence.tex:242` (`\label{thm:raw-first-half-code}`).

Full-range hit codes with actual first-half shell to original PCT hit codes, retaining raw scale separately.

The exact gate isq(2C0-Theta0)>=Omega0+2C0 with complete sign cases. Decode all fourdigits then recode in the other radix; inverse repeats this in reverse, with exact differencecF-cH=2(A-1)LambdaH+(q-1)j/2.

Both original radices, signs, bounds and tuple coordinates remain. The p13 code36 versus108 fixture detects failure of identifying the integers and verifies the surviving bijection.

### R44 — Decoded indicator, least hit and smaller layers

Evidence: `raw_scale_hit_incidence.tex:294` (`Define the total bit`).

Every code in the full PCT rectangle, including failures, to the total bit and least-hit selector with its explicit sentinel.

Decoded a(c),R(c),A(c),B(c),epsilon(c) define all three tests. Prefix products give the exact least hit orN_H. A-layer occupancy is equivalent to selector<r0LambdaH; a particular witness only supplies its proved upper bound.

The final total map is defined on arbitrary codes. Fixed seed data are not substituted for decoded data, and no success is manufactured at a failure sentinel.

### R45 — All scaled input-prime-hit fibres and Boolean selections

Evidence: `raw_scale_hit_incidence.tex:412` (`\label{thm:raw-scale-hit-incidence}`).

The finite original graph with retained fixedseed,k,X,q and all original input/output constraints, to primitive incidence, either radix and complete ordered witness data.

Graph projection is an inverse; forgettingk has its computed gcd square-divisor fibre and section1. Fixedscale/q input fibres retain all rootCRT branches and bounds. All predicates select intersections in these same full fibres.

No input or scale is erased without its complete fibre. Counts agree by either projection, both radices preserve every truth vector, and a fixed rawscale failure atq109 is retained despite primitive success.

### R46 — Full oriented shell coefficient obstruction and exact ordered witness return

Evidence: `counterexample_sieve/all_shell_no_hit.tex:80` (`\label{thm:shellwise-no-hit}`).

Original full valuation box to E_a and M_a; tags (0,u,sigma), sigma=0,1, and (1,u,0) map to all positive ordered (a,y,z).

Chart A=a/g,B=u/g,D=g^2/u,C=(A+p^(1-epsilon)B)/R retains y0=p^epsilon ACD,z0=pBCD and exterior swap. The exact inverse at i=v_p(d_y)=0,1,2 is (0,a^2/d_y,0),(1,pa^2/d_y,0),(0,d_y/p^2,1). Residual d_y maps inversely to (a,(S+d_y)/R,(S+S^2/d_y)/R). Every oriented fibre is singleton. Order-forgetting fibres have exactly two points; middle complement swaps A,B while retaining C,D.

The original reciprocal numerator is pC, not pCD. Every exterior orientation and both middle divisors are retained, with full unit gate converses, positive integer return, ordered count2|E|+|M| and unordered count|E|+|M|/2. PR-006/007 record the errors missed in the prior pass.

### R47 — Global necessary-and-sufficient no-hit obstruction

Evidence: `counterexample_sieve/all_shell_no_hit.tex:296` (`\label{thm:global-no-hit}`).

The product of exact per-shell zero indicators and the nonnegative sum Q_p, retaining every a and both channel targets.

O_p=1 iff every shell has both target coefficients zero iff Q_p=0 iff the original prime has no ordered ES witness. The residual-three/seven corollary is an immediate necessary consequence.

This is a finite exact obstruction only; it makes no density, infinitude, or universal occupancy claim.

### R48 — Global obstruction projects to exact R3/R7 rejection

Evidence: `counterexample_sieve/all_shell_no_hit.tex:331` (`\label{cor:global-r3-r7}`).

All-shell no-hit primes to the previously defined residual-three and residual-seven shell predicates.

Coefficient-zero fibres imply the exact empty-fibre predicates on the corresponding shells, preserving their complete labels and residual conditions.

The implication is one-way and does not identify the all-shell sieve with either restricted two-shell test.

### R49 — NS-DIRECTIONAL

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:34` (`\label{thm:ns-directional-intertwining}`).

Complex trigonometric polynomials on T^2=(R/Z)^2; P_0 has zero constant coefficient. Original J=((3,1),(1,5)), v_r=(1,1-sqrt2), v_t=(sqrt2-1,1), m>=0. Codomain: Same polynomial space; P_m image has frequencies (J^m)^T Z^2. D_v image is exactly P_0.

P_m f(Y)=f(J^mY); Fourier frequency n -> (J^m)^T n. D_v has multiplier 2*pi*i*(v dot n). D_v P_m=lambda^m P_m D_v. Inverse: P_m inverse reads the image coefficient at (J^m)^T n. D_v^-1 divides each nonzero mode by 2*pi*i*(v dot n); its intertwining factor is lambda^-m. Fibres: P_m fibres are singleton on its stated image and empty outside; D_v fibre at f in P_0 is D_v^-1 f+C. Kernel/zero fibre: ker P_m=0; ker D_v=constant polynomials, with image and all fibres proved.

Original eigenvector lengths, coordinates, signs, eigenvalues and Fourier constant are retained; finite support makes the inverse calculation exact without analytic convergence hypotheses.

### R50 — NS-TORUS-FIBRES

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:93` (`\label{thm:ns-torus-fibres}`).

Original unit-period torus T^2 and each retained N-torsion domain (Z/N)^2, N>=1. Codomain: T^2, and finite image I_N={(a,b):gcd(N,14)|(5a-b)}.

Phi(x,y)=(3x+y,x+5y); torsion coordinates j -> j/N commute with Phi. Inverse: Torus x=(5*a_tilde-b_tilde+k)/14, y=a_tilde-3x, k=0,...,13. Torsion x=x0+k*N/g, y=a-3x where x0 solves the divided congruence, including N/g=1. Fibres: Exactly 14 torus points at every target; g=gcd(N,14) torsion points on I_N and none outside. Kernel/zero fibre: ker Phi={(k/14,-3k/14)}; ker J_N={(kN/g,-3kN/g)}; all image tests use the original target representatives.

All lift labels, denominators 14 and N, inverse unit factors, and target coordinates remain; no finite restriction is assumed surjective.

### R51 — NS-TORUS-AVERAGE

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:151` (`\label{thm:ns-torus-average}`).

Functions on original N-torsion; separately C^D and C^H for any finite subgroup D of T^2 and H=Phi^-1 D. Codomain: Exact finite means, C^H and C^D with their stated probability inner products.

Finite pullback mean is g/N^2 times sum over I_N. P f(h)=f(Phi h); L v(d)=(1/14)*sum_{Phi h=d}v(h). Inverse: LP=identity; on deck-invariant v the unique P inverse is Lv. PL is the deck-orbit projection. Fibres: L^-1(f)=Pf+ker L; P^-1(v)={Lv} for deck-invariant v and empty otherwise. Kernel/zero fibre: ker L consists of functions whose sum on each complete 14-point fibre is zero. Finite alias frequencies are (kN/g,-3kN/g).

|H|=14|D| proves exact adjunction and all averaging factors; fixed N-torsion mean equals the full mean for all f exactly when g=1.

### R52 — NS-TORUS-POWERS

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:237` (`\label{prop:ns-torus-powers}`).

m>=0, original J^m acting on T^2 and s independent blocks; separately original N-torsion. Codomain: Every exact torus target fibre and the complete recursive N-torsion fibre.

[k] in Z^2/J^m Z^2 maps to J^-m(d_tilde+k) modulo Z^2; block products retain each k label. Inverse: A real lift h_tilde gives [J^m h_tilde-d_tilde]. Fixed N-torsion fibres iterate all one-step inverse branches. Fibres: 14^m points per torus target and 14^(m*s) per product target; finite fixed-torsion fibres are exactly the disjoint recursion S_j, not an assumed g^j count. Kernel/zero fibre: Zero-target fibres are the same quotient parametrizations; the fixed N recursion includes empty branches.

All original row powers, quotient labels and multiplicities are retained; N=2 has successive kernel sizes 2,4,4 and proves the stated obstruction.

### R53 — NS-FINITE-DUAL

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:279` (`\label{lem:ns-finite-dual}`).

Any finite abelian multiplicative G and an ordered retained generating tuple g_1,...,g_s. Codomain: Actual multiplicative dual Hom(G,S^1), embedded in T^s by character evaluation angles.

E(chi)_j is the unique angle class whose exponential is chi(g_j). Extension from B by g retains all r roots z^r=chi(g^r). Inverse: On the image, g_1^n1...g_s^ns maps to exp(2*pi*i*sum n_j theta_j); all relation equations characterize precisely its well-defined domain. Fibres: E fibres are singleton on the relation locus; each character extension has all r distinct root choices; |dual G|=|G|. Kernel/zero fibre: ker E is the identity character; character sum at u is zero exactly for u!=1 and equals |G| at u=1.

Every generator and relation is retained. Character separation and exact orthogonality are proved by extension and translation, without a cyclic-group assumption.

### R54 — NS-ES-PACKET

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:342` (`\label{thm:ns-es-packet}`).

Actual dual square of finite abelian G; in the ES application G=U(R), original p,a,R and all prime exponent bounds for a and any second packet b with factors units modulo the same R. Codomain: Same dual square and exact original labelled divisor-packet coefficient fibres.

J_G(chi,psi)=(chi^3 psi,chi psi^5); Fourier pullback (u,v) -> (u^3v,uv^5). Packet F_{a,g}=chi(g^-1)*product sum_{e=0}^{2v_l(a)}chi([l])^e. Inverse: Target (alpha,beta) has psi=alpha chi^-3, with every fourteenth root chi^14=alpha^5 beta^-1. A packet divisor recovers all valuations; a joint alias index is u=g^-1[U_a(e)]. Fibres: Joint mean sums the full exponent pairs [U_a(e)]=gu, [U_b(f)]=hu^-3 over u^14=1. Row (A_m,B_m) retains exactly u^gcd(A_m,B_m)=1. Kernel/zero fibre: ker J_G={(chi,chi^-3):chi^14=1}; image consists exactly of targets admitting the stated fourteenth root.

The single marginal count remains c_a(g); all alias coefficients are explicit nonnegative integers with complete valuation inverses and original factors retained.

### R55 — NS-ES-COVER-REPAIR

Evidence: `ns_operator_bridge/torus_cover_lemma.tex:455` (`\label{cor:ns-es-cover-repair}`).

D=E_2(dual G^2), H_m=((J^m)^(oplus s))^-1(D), original m and retained generator tuple. Codomain: Same dual square and its original packet counts through E_2^-1.

z -> E_2^-1((J^m)^(oplus s)z); exact mean on H_m equals mean on the original dual square. Inverse: Every target has all product quotient lifts from T8 and the exact E_2 inverse supplied by T10. Fibres: 14^(m*s) preimages per target; |H_m|=14^(m*s)|G|^2. Kernel/zero fibre: The zero-target fibre is the product cover kernel; all other fibres are its exact translates.

p=13,a=4 keeps divisors1,2,4,8,16 and counts3,2: uncorrected joint mean13 versus target4 and second-power marginal5 versus target2; full preimages restore2 and4 at every m.

### R56 — NS-LABELLED-PACKET

Evidence: `ns_operator_bridge/sieve_character_cover.tex:53` (`\label{lem:ns-labelled-character-packet}`).

Original shell p,a,R and ordered distinct actual primes l_j with valuations v_j; additive dual Hom(U(R),R/Z), independent formal variables X_j. Codomain: Polynomial C_{a,t} whose monomials are exactly full original divisor valuations attaining t.

C_{a,t}=|dual G|^-1 sum_chi exp(-2*pi*i*chi(t))*product_j sum_{e=0}^{2v_j}X_j^e exp(2*pi*i*e*chi([l_j])). Inverse: Each retained monomial gives u=product l_j^e_j, and u returns its full valuation vector. Additive/multiplicative dual conversion is exp(2*pi*i chi), with unique class inverse. Fibres: Each original exponent vector appears with coefficient1 exactly on the target residue fibre and otherwise0; distinct original vectors remain distinct monomials. Kernel/zero fibre: Character orthogonality kills exactly the nonidentity residual. The two targets t=-4^-1 and -a give the complete E_a and M_a fibres.

Original exponent caps, prime labels, unit tests and 2*pi*i are retained; evaluation at X=1 counts but does not replace the recoverable polynomial fibre.

### R57 — NS-COSET-CORRECTION

Evidence: `ns_operator_bridge/sieve_character_cover.tex:120` (`\label{thm:ns-character-coset-correction}`).

Actual additive dual square and retained representative table R for dual G/14 dual G; delta=|dual G[14]|. Codomain: Same dual square and exact quotient dual G/14 dual G.

J(alpha,beta)=(3alpha+beta,alpha+5beta); q(gamma,eta)=[eta-5gamma]; T(alpha,beta,rho)=(3alpha+beta,alpha+5beta+rho). Inverse: For each target choose its unique recorded rho representing [eta-5gamma]; enumerate every alpha solving14alpha=5gamma-eta+rho; beta=gamma-3alpha. Fibres: J fibres are empty unless eta-5gamma lies in14 dual G and otherwise a delta-point kernel coset; T is onto with exactly delta points at every target. Kernel/zero fibre: ker J={(tau,-3tau):14tau=0}; ker q=image J; quotient has exactly delta classes.

The representative table and every root remain coordinates; averaging over all domain labels divides by delta|dual G|^2 and exactly returns the original uniform target mean.

### R58 — NS-ALIAS-REMOVAL

Evidence: `ns_operator_bridge/sieve_character_cover.tex:186` (`\label{thm:ns-sieve-alias-removal}`).

Original same-shell pairs of valuation vectors e,f and targets t1,t2, with independent X,Z variables and actual additive dual square. Codomain: Joint target packet polynomials, before and after the retained coset correction.

Uncorrected mean is sum_{g^14=1} C_{a,t1g}(X)C_{a,t2g^-3}(Z); corrected mean over T is C_{a,t1}(X)C_{a,t2}(Z). Inverse: Each monomial recovers e,f and u,v; the unique alias parameter is g=u*t1^-1, k=v*t2^-1=g^-3. Fibres: Uncorrected fibre contains precisely all retained exponent pairs in the listed torsion-indexed target fibres; correction preserves precisely g=k=1 with full original monomials. Kernel/zero fibre: The two pullback character residuals are g^3k and gk^5. Coset orthogonality removes nonidentity k in G[14]; gcd(3,14)=1 then removes nonidentity g.

No original monomial is identified with another; all constants, signs, roots and finite exponent boxes remain. The exact obstruction and constructive repair replace any unsupported uniformity claim.

### R59 — NS-FINITE-HISTORY

Evidence: `ns_operator_bridge/sieve_character_cover.tex:243` (`\label{cor:ns-finite-history-sieve}`).

For k>=0, original z0 in dual G^2 and every retained sequence rho0,...,rho_(k-1) in the representative table. Codomain: Original terminal dual state and its complete valuation packet coefficients.

z_(j+1)=J z_j+(0,rho_j); terminal z_k=J^k z0+sum_j J^(k-1-j)(0,rho_j). Inverse: At each reverse step the terminal coset determines rho uniquely, and every delta predecessor root is retained; iterate to recover every full history. Fibres: Exactly delta^k histories at every terminal point, including a singleton at k=0. Kernel/zero fibre: Terminal zero has the same exact inverse recursion; this labelled history map is not incorrectly assigned a group kernel when the representative table is not a group.

Corrected finite-history average returns the original labelled packet. Each monomial then uses the full oriented no-hit inverse, retaining exterior sigma and both middle divisors, so q_a,p, Q_p and O_p remain unchanged.

### R60 — Additive and multiplicative dual conventions

Evidence: `ns_operator_bridge/sieve_character_cover.tex:22` (`The exact change from the preceding subsection`).

Hom(G,R/Z) and Hom(G,S^1) on the same actual finite group G.

chi maps to g -> exp(2*pi*i*chi(g)); the inverse takes the unique class in R/Z at every g. Identity and product equations are preserved; both compositions are identities and all fibres singleton.

The original unit-period torus and the literal Fourier factor are retained; no unstated switch of dual convention changes a sign or denominator.

### R61 — Every arithmetic evaluation and the source torus operator

Evidence: `ns_operator_bridge/sieve_character_cover.tex:101` (`E_g:\widehat G^2`).

Actual additive dual square and the family of all evaluation-coordinate tori indexed by each retained unit g.

E_g(alpha,beta)=(alpha(g),beta(g)); the family is injective, with image exactly the two character tables and inverse reading those tables. E_g J=J E_g holds component by component.

A single evaluation is not asserted injective. Retaining the complete evaluation family supplies the strongest exact coordinate morphism used by the arithmetic restriction and preserves every original prime residue and both targets.

### R62 — NS-CONTINUOUS-TRANSFER

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:18` (`\label{thm:ns-continuous-transfer}`).

C-infinity complex functions on the original unit-period torus, m>=0 and complete K_m=ker J^m; original v_r,v_t and their eigenvalues. Codomain: Same smooth space; P_m image is exactly the K_m-invariant functions and Q_m is the projection to that image.

P_m f(Y)=f(J^mY); L_m g(X)=14^-m sum_(J^mY=X)g(Y); Q_m g(Y)=14^-m sum_(a in K_m)g(Y+a). Inverse: L_m P_m=1 and P_m L_m=Q_m. On invariant functions P_m^-1=L_m. On characters L_m e_n=e_k exactly when n=(J^m)^T k and is zero otherwise. Complete fibres: L_m^-1(f)=P_m f+ker L_m; P_m^-1(g)={L_m g} if Q_m g=g and is empty otherwise; Q_m^-1(h)=h+ker L_m if Q_m h=h and is empty otherwise. Kernel: ker L_m=ker Q_m consists exactly of smooth functions whose sum on every full covering fibre is zero; ker P_m=0.

All 14^m fibre points, original frequency n and returned k are retained. Haar, adjoint and product identities hold exactly; derivative and inverse transfer factors are lambda^-m and lambda^m with 2*pi*i unchanged.

### R63 — NS-REVERSE-MEAN

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:109` (`\label{thm:ns-reverse-mean-correction}`).

Smooth extended residuals E_i on U times the original torus, fixed Q>0, h_NS and c_i=T_g^i Q^(1+h_NS), with every slow parameter and band index retained. Codomain: Smooth zero-mean common-band correction coefficients and their exact absolute representatives.

C_i(E)=-c_i^-1 D_t^-1(E-Pi E); c_i D_t C_i(E)=-E^circ. For i>=j, C_j(P_(i-j)E_i)=P_(i-j)C_i(E_i). Inverse: Given zero-mean V, all residuals are E=-c_i D_t V+b for arbitrary slow b. A common coefficient descends iff Q_(i-j)E_j=E_j; its inverse is E_i=L_(i-j)E_j. Complete fibres: Every correction fibre retains the full free slow function b. Pullback fibres are singleton on the deck-invariant image and empty outside, with all common sums and overlap equations retained. Kernel: ker C_i is precisely the torus-independent slow functions; no auxiliary mean is inferred from a sampled or evaluated representative.

The full original factor is Q^A Q^-(2A+1/2) T_g^-i=Q^-1-h T_g^-i=c_i^-1. Fixed Q is retained in common-band covariance and all signs remain.

### R64 — NS-PHYSICAL-EVALUATION

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:189` (`\label{thm:ns-physical-evaluation-fibre}`).

Smooth extended coefficients on physical slow U times the original torus; r>0, phi(r,t)=v_r r^d_r+v_t t and original d_r>0, Q,h_NS. Codomain: Smooth physical coefficients; jointly, arbitrary pairs (physical coefficient, torus mean) on the same retained slow domain.

E_i F(r,z,t)=F(r,z,t,J^i phi(r,t)); E_i=E_j P_(i-j). The literal physical chain rules retain T_g^i, Lambda^i d_r r^(d_r-1), and the slow derivatives. Inverse: Physical f has constant-in-torus extension. For joint target (f,b), fix nonzero n, a=e_n(J^i phi), and F_(f,b)=b+(f-b)e_n/a; this is an explicit right inverse. Complete fibres: E_i^-1(f)=f+ker E_i. Joint fibres are F_(f,b)+(ker E_i intersect ker Pi), including a real right inverse by taking real parts. Kernel: ker E_i is the graph-vanishing smooth functions. H=e_n-a belongs to it while Pi H=-a !=0, proving mean does not descend through evaluation.

Chart time is -Q^h partial_T+c_i D_t, radial operator is partial_R+Lambda^i Q^(d_r/2)d_r R^(d_r-1)D_r, and z operator is Q^h partial_Z. No axis extension or free mean is deleted.

### R65 — NS-REVERSE-SAMPLING

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:267` (`\label{thm:ns-reverse-sampling-fibres}`).

Finite trigonometric polynomials with original integer frequencies, N>=1, and separately every retained finite original spectrum Omega. Codomain: Grid functions on D_N=T[N]^2; restricted image consists exactly of DFT coefficients zero in every residue class absent from Omega.

R_N f=f|D_N; DFT at r is sum_(n congruent r mod N)a_n. R_N P_m=J_N^(m*) R_N. Inverse: Retain n_r in each occupied class and arbitrary b_n for n != n_r; set a_n=b_n and a_(n_r)=qhat(r)-sum_(other n in Omega_r)b_n. Complete fibres: The displayed independent free coefficients give every restriction fibre. Restriction is bijective onto its image exactly for singleton occupied residue classes. Kernel: Full kernel is zero coefficient sum in every residue class. Derivative descends iff Omega residues are distinct; inverse descends iff residues of Omega minus original zero are distinct.

Colliding character differences explicitly prove necessity with the nonzero original multipliers 2*pi*i(v dot n) and their reciprocals. Exact conjugation proves the converse and uniqueness on each image. All original labels and Haar-zero restrictions remain.

### R66 — NS-REVERSE-LIFTED-INVERSE

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:378` (`\label{thm:ns-reverse-lifted-inverse}`).

Retained original Omega injective modulo N and original zero-Haar subspace; separately all smooth original torus functions with their weighted Fourier norms. Codomain: Exact original polynomials reconstructed from the grid image; full-preimage data on H_(m,N)=(J^m)^-1 D_N of size 14^m N^2.

I_Omega q=sum_(n in Omega)qhat([n])e_n; lifted coefficient is (14^m N^2)^-1 sum_Y P_m f(Y)e_-((J^m)^T n)(Y). Inverse: R_N and I_Omega are inverse on their stated domains. The transported integer frequency has inverse (J^-m)^T on (J^m)^T Omega; its actual inverse multiplier is 1/[2*pi*i lambda^m(v dot n)]. Complete fibres: All polynomial/grid-image fibres are singleton; each target grid point retains its full 14^m covering fibre. Original spectrum collisions remain outside this injective restriction domain. Kernel: The original zero coefficient is excluded for the inverse, even if a nonzero original mode has constant grid values. The quadrature error is exactly sum_(n in N Z^2 minus zero)fhat(n).

The error bound (1+4*pi^2 N^2)^(-s/2)||f||_(A^s) retains original units and is independent of m. Full preimage labels preserve the finite recovery instead of silently changing the grid or original spectrum.

### R67 — NS-SHARP-DENOMINATOR

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:21` (`\label{thm:ns-sharp-denominator}`).

Every nonzero original integer k=(m,n), original paired vectors (v_r,w_r) and (v_t,w_t), c=sqrt(4+2sqrt2), d=sqrt(4-2sqrt2). Codomain: Original real directional symbols and their nonzero integer algebraic norm, with an explicit Pell family realizing the sharp infimum.

The products are (m+n)^2-2n^2 and (n-m)^2-2m^2; k maps to (v dot k,w dot k), and |v dot k| |k|>1/c. Inverse: The complete real-coordinate inverse is k=(v dot k)v/d^2+(w dot k)w/c^2; on the integer image it recovers the original m,n exactly. Complete fibres: The paired-symbol map has singleton fibres on its image. The Pell recurrence p_(j+1)=p_j+2q_j, q_(j+1)=p_j+q_j retains both integers and gives k_r=(p-q,q), k_t=(q,q-p). Kernel: Neither original directional symbol vanishes on a nonzero integer vector. Their conjugate norm is a nonzero integer with absolute value at least one.

K_j^2=alpha^(2j)/c^2+alpha^(-2j)/d^2 and |v dot k_j|^2 K_j^2=1/c^2+alpha^(-4j)/d^2 prove sharpness and nonattainment without rescaling either vector.

### R68 — NS-FOURIER-ANALYTIC

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:120` (`\label{lem:ns-fourier-analytic-bridge}`).

Actual smooth period-one functions, and separately zero-mean coefficient arrays with finite displayed W^(m+2) weighted square sum; W(k)=1+4*pi^2|k|^2. Codomain: Original Fourier arrays and actual C^m functions reconstructed by absolutely and uniformly differentiated Fourier series.

f maps to integral f(Y)e_-k(Y)dY; derivative coefficients are (2*pi*i k_1)^a(2*pi*i k_2)^b fhat(k). H^r<=3^(r/2)C^r and C^m<=sqrt(S)H^(m+2) on the specified zero-mean arrays. Inverse: The inverse is sum_k fhat(k)e_k with each derivative summed in the original coordinates. Uniform Fejer reconstruction proves the original smooth function is recovered. Complete fibres: Fourier reconstruction has singleton fibres on its actual image; no merely formal array is silently identified with a function without its convergence proof. Kernel: Zero coefficient arrays reconstruct only the zero function. S=sum_(k !=0)W(k)^-2 is positive and at most 3/(4*pi^4).

Bessel, integration by parts, the full 8j lattice-shell count and Cauchy-Schwarz retain all constants and prove the exact analytic type bridge needed for the inverse.

### R69 — NS-SHARP-SMOOTH-INVERSE

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:207` (`\label{thm:ns-sharp-smooth-inverse}`).

H^(s+q)_0 for real s and integer q>=1 in the exact W-weight convention; actual smooth zero-mean data and their explicit continuous distribution realizations. Codomain: H^s_0 inverse coefficients; actual smooth zero-mean solutions and zero-mean distributions solving D_v^q u=f.

T_v^q f has zero original frequency zero and coefficient fhat(k)/(2*pi*i v dot k)^q for every original k !=0. Inverse: D_v^q multiplies by (2*pi*i v dot k)^q. On smooth zero-mean spaces the two maps are inverse. On the specified distribution datum domain, the continuous-functional coefficient argument proves the same equation and uniqueness. Complete fibres: Every smooth D_v^q fibre at zero-mean f is T_v^q f+C; the zero-mean fibre is singleton. Each distributional zero-mean solution is uniquely forced by all original coefficients. Kernel: Smooth kernel is exactly the constants and image is the full smooth zero-mean space. Every nonzero original mode remains invertible.

Exact H^(s+q)_0 to H^s_0 norm is (c/(4*pi^2))^q. Pell characters prove no smaller Sobolev derivative loss. The explicit test-function pairing and differentiation sign retain the distribution type without an assumed analytic bridge.

### R70 — NS-CM-IMPROVEMENT

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:307` (`\label{thm:ns-cm-improvement}`).

Original zero-mean C^(m+q+2) functions for integers m>=0,q>=1, with every mixed coordinate derivative through the stated order. Codomain: Actual C^m inverse functions in the unchanged period-one coordinate norm.

T_v^q is the original Fourier multiplier; its C^m norm is bounded by (c/(4*pi^2))^q sqrt(S) times H^(m+q+2), then by C_(m,q) times C^(m+q+2). Inverse: The coefficient inverse remains D_v^q on its exact returned range, with the original zero coefficient retained; no inverse of a scalar norm inequality is asserted. Complete fibres: T_v^q has singleton fibres on this zero-mean input domain, since every nonzero multiplier is nonzero; its stated range retains the complete original Fourier coefficients. Kernel: The only datum with zero inverse is zero. The explicit norm constant is C_(m,q)=(c/(4*pi^2))^q 3^((m+q+2)/2)sqrt(S).

For q=1 the source temporal step uses m+3 input derivatives; for general q the repeated radial inverse uses m+q+2. No optimal C^m count or complete nonlinear PDE conclusion is claimed.

### R71 — NS-INVERSE-PARAMETERS

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:348` (`\label{prop:ns-inverse-parameters}`).

Jointly smooth parameter-dependent original zero-mean data; source common y=J^iY and absolute Y tori with their separately defined means, derivatives and inverses; smooth shell-supported cutoff data at R>0. Codomain: Jointly smooth inverse coefficients, their exact parameter derivatives, and the source temporal and desired/actual axial corrections in both retained torus coordinate systems.

Parameter differentiation commutes with T_v^q, preserves zero slices and reality. D_t^Y P_i=T_g^i P_i D_t^y and T_t^Y P_i=T_g^-i P_i T_t^y. Delta v=-c_i^-1 T_t^y E_theta^circ. Inverse: The common correction satisfies c_i D_t^y Delta v=-E_theta^circ; the exact absolute representative is P_i Delta v=-Q^-1-h T_t^Y P_i E_theta^circ. Inverse/pullback fibres are those already proved on the exact zero-mean/deck-invariant domains. Complete fibres: The directional inverse has singleton zero-mean fibres, parameter by parameter; the retained source residual has the full free-mean fibre from NS-REVERSE-MEAN. The actual axial update is gamma_d+a with a=-A1 gamma_d, not a lost coordinate. Kernel: Support means entire parameter slices remain zero. The original nonzero Fourier symbols give the inverse kernel; no zero or flatness claim is made for the separate cutoff remainder a.

A1 retains R^-1 partial_R chi_m times integral Rprime f(Rprime,Z,T,y+M((Rprime)^d_r-R^d_r)v_r)dRprime, M=Lambda^i Q^(d_r/2). Actual fast cancellation retains c_i D_t^y a, and both slow terms -Q^h partial_T Delta v and -Q^h partial_T Delta gamma remain.

### R72 — Fixed-scale common and absolute correction coordinates

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:135` (`c_j=T_g^{-\Delta}c_i`).

Common-band coefficients with fixed Q>0, retained i>=j>=0, Delta=i-j and c_i=T_g^i Q^(1+h).

The exact pullback P_Delta retains frequency (J^Delta)^T n. Inverse covariance contributes T_g^-Delta while c_j^-1=T_g^Delta c_i^-1; the factors multiply to c_i^-1. Descent test Q_Delta E_j=E_j has inverse L_Delta and its full known fibre.

The full Q exponent calculation proves the actual signed physical coefficient. Neither a different Q nor a different band normalization is silently substituted.

### R73 — Physical slow chart and auxiliary directional derivatives

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:216` (`Q^{1+h_{\rm NS}}\partial_t`).

Original r,z,t with r>0 and Q>0; R=r/sqrt(Q), Z=z/Q^(1/2-h), T=(1-t)/Q, retained torus y=J^i phi(r,t).

Chart inverse is r=sqrt(Q)R,z=Q^(1/2-h)Z,t=1-QT. The graph chain rule gives -Q^h partial_T+c_i D_t, partial_R+Lambda^i Q^(d_r/2)d_r R^(d_r-1)D_r, and Q^h partial_Z. Coordinate chart fibres are singleton.

Every slow and fast term survives, including the negative time sign and both vector eigenvalues. The domain avoids an unproved axis-regularity extension.

### R74 — Complete joint physical-evaluation and auxiliary-mean fibre

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:252` (`F_{f,b}(r,z,t,Y)=b(r,z,t)`).

Extended smooth coefficient F on U times torus and arbitrary smooth desired pair (f,b) on the original slow domain.

Fix nonzero n and a=e_n(J^i phi). F_(f,b)=b+(f-b)e_n/a has exactly evaluation f and mean b; its entire fibre is F_(f,b)+(ker E_i intersect ker Pi). H=e_n-a proves mean cannot factor through evaluation.

The obstruction is followed immediately by the strongest displayed positive correspondence: the joint map is onto every pair, with all fibres and real right inverse explicitly given.

### R75 — Sharp derivative and inverse descent on a retained spectrum

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:330` (`For the sharp restricted derivative criterion`).

Original finite Omega and its zero-Haar subspace with original frequency zero removed, both mapped by the same R_N.

For each colliding distinct n,nprime, e_n-e_nprime lies in the restriction kernel while its derivative has multiplier difference 2*pi*i v dot(n-nprime) !=0. On the nonzero domain the reciprocal difference is also nonzero. Distinct residues give the converse by R_N D_v R_N^-1 or R_N D_v^-1 R_N^-1 on the exact image.

Derivative uses all Omega, inverse only Omega minus original zero; the empty image is included. Every original integer frequency is retained and the necessary-and-sufficient criteria are proved.

### R76 — Smooth Haar integration and full-preimage quadrature

Evidence: `ns_operator_bridge/reverse_proof_transport.tex:449` (`For smooth $f$ the Fourier series is absolutely summable`).

Actual smooth torus f with original weighted Fourier norm A^s, N>=1,m>=0 and H_(m,N) full preimage of D_N.

Each target has 14^m points, so the full-preimage average of P_m f equals the original grid average. Termwise exact character averaging leaves sum_(n in N Z^2)fhat(n); removing original zero gives the complete error functional.

All original frequencies in the error fibre are retained. The explicit bound uses |n|>=N and has no hidden cover-level factor; convergence is proved by integration by parts and the full lattice-shell count.

### R77 — Weighted coefficient arrays and actual distributions

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:271` (`To make the distributional assertion explicit`).

Returned H^s array u, original smooth test functions with C^M seminorms, and complex-linear distributions with coefficient u(e_-k).

u(phi)=sum_k uhat(k) phihat(-k). Polynomial growth and M>r+2 give the stated finite C^M bound. D_v u(phi)=-u(D_v phi) produces coefficient 2*pi*i(v dot k)uhat(k). Smooth Fourier convergence in every seminorm proves coefficient uniqueness.

The actual continuous functional, full derivative equation, and unique zero-mean inverse are proved. No formal-array-to-distribution identification is left untyped; PR-008 preserves the incoming gap.

### R78 — Exact Sobolev inverse and coordinate differentiability estimate

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:333` (`Apply (S9) to the inverse coefficients`).

Original zero-mean C^(m+q+2) data, embedded by actual Fourier coefficients in H^(m+q+2), and returned H^(m+2) inverse coefficients.

Composition of the proved Fourier map, exact inverse multiplier and uniform reconstruction gives C^m output with constant (c/(4*pi^2))^q 3^((m+q+2)/2)sqrt(S). All maps retain original coefficients and have the inverse/fibres already stated on their precise images.

The original period, vector norms, 2*pi factors, lattice constant and finite derivative orders are retained. Optimal Sobolev loss is not conflated with an unproved optimal C^m count.

### R79 — Desired and actual axial correction with the original cutoff

Evidence: `ns_operator_bridge/reverse_smooth_inverse.tex:408` (`(A_1f)(R,Z,T,y)`).

Smooth zero-mean shell-supported data with R>0, original M=Lambda^i Q^(d_r/2), torus-independent smooth chi_m and locally compact radial support.

gamma_d=-c_i^-1 T_t^y E_z^circ; a=-A1 gamma_d with the full shifted radial integral; Delta gamma=gamma_d+a. Retaining the pair (gamma_d,a) gives exact graph projection and its reading inverse. Linearity gives c_i D_t^y Delta gamma=-E_z^circ+c_i D_t^y a.

Local differentiation over the retained compact radial interval proves smoothness. The actual fast remainder and -Q^h partial_T Delta gamma remain; neither a zero remainder nor the source full flatness/PDE result is assumed.

### R80 — BOUNDARY-FOUR-RATIONAL

Evidence: `boundary_swap/boundary_swap_completion.tex:41` (`\label{thm:boundary-four-rational}`).

Complete raw records (p,j,a,R,S,A,B,C,D,epsilon), p=12h+1 prime, 0<=j<6h, ABD=a, A,B,D positive integers, epsilon0 or1, C=(A+p^(1-epsilon)B)/R positive rational. Codomain: The same full rational record space, all four labelled candidates, their original ordered rational witnesses and both oriented integer residuals.

T_(s,t) swaps A,B iff s1, sets eta=epsilon+t-2epsilon t and forces C_(s,t)=(alpha+p^(1-eta)beta)/R while retaining p,j,a,R,S,D. Inverse: Each T_(s,t) is its own inverse; the two retained bits compose by addition modulo2. The full source/bit/target graph has inverse projection to the original source and both bits. Complete fibres: Each individual map has singleton fibres. Rational orbits have four members if A!=B and two if A=B; stabilizers are {(0,0)} and {(0,0),(1,0)} respectively. The forgotten raw-scale fibre is every k>0 with k^2|D0. Kernel: Fixed-point loci are exactly those given by the retained tag and coordinate equality; no additive kernel is assigned to the record action.

Witnesses are (a,p^eta alpha C D,p beta C D), with d_y=p^eta alpha^2D,d_z=p^(2-eta)beta^2D,S^2 product. Raw scale (A,B,C,D)=(kA0,kB0,kC0,D0/k^2) preserves every original ordered coordinate and commutes with all four maps.

### R81 — BOUNDARY-OBSTRUCTION

Evidence: `boundary_swap/boundary_swap_completion.tex:149` (`\label{thm:boundary-obstruction-fibres}`).

Original integral raw source records and all four rational candidates with every numerator, tag, unit factor and shell residual R retained. Codomain: Exact integral-return subsets; otherwise original positive rational targets with obstruction in (Q/Z)[R] and its precise additive order.

The same-tag exterior numerator is (1-p^2)B modR; tag changes give (1-p)B or signed (p-1)B. psi_R([n])=n/R+Z maps the literal numerator to C_target+Z. Inverse: psi_R is an isomorphism with inverse the class of R times a rational representative. Each integral T_(s,t) restriction is its own inverse; rational targets retain the unrestricted inverse. Complete fibres: For m in the table, g=gcd(R,m),delta=R/g: psi_R composed with [m] has full fibre [b0]+delta Z/RZ on its image and empty fibres elsewhere. Kernel: That multiplier map has kernel delta Z/RZ and image (Q/Z)[delta], including m0. The unit input B has exact output order delta.

All C_target,y_target,z_target have the same exact reduced denominator: 1 on identity/middle swap, R/gcd(R,p^2-1) on exterior swap, and R/gcd(R,p-1) on tag changes. Every original fraction and every failed integral candidate remains.

### R82 — BOUNDARY-ORBIT-CODE

Evidence: `boundary_swap/boundary_swap_completion.tex:258` (`\label{thm:boundary-orbit-code}`).

Original integral raw records in either half radix (J,M)=(3h,6h),j<3h or full radix (6h,9h),j<6h; coprime digits and raw k retained. Codomain: Original code interval 0<=c<M Lambda, complete integral orbits and their unique minimum-code members at retained k.

c=(A0-1)Lambda+2M j+2(B0-1)+epsilon. Difference at T_(s,t) is s(B0-A0)(Lambda-2)+(eta-epsilon). Select the displayed minimum over the exact integral orbit. Inverse: Successive Euclidean division by Lambda,2M,2 recovers A0,j,B0,epsilon; the hit budget/gate recover D0,C0 and k recovers every raw coordinate. Complete fibres: Code plus k has singleton fibres on the hit domain. The minimum-selection map at retained k has precisely the full listed integral orbit as its fibre. Forgetting k adds the disjoint orbits over all k^2|D0. Kernel: All four orbit rows retain their exact integrality gates; stabilizers give every coincidence. A lower rational code is not asserted to be an integral hit when the shell obstruction is nonzero.

R|p-1 forces tag0 and A0<B0 at minima; middle swaps lower iff A0>B0, and exterior swaps give lower integral hits iff additionally R|p^2-1. Original shell ranges, digits, witness return and rational failed fibres remain.

### R83 — BOUNDARY-R3-FIRST-CODE

Evidence: `boundary_swap/boundary_swap_completion.tex:384` (`\label{thm:boundary-r3-global-minimum}`).

All original p=12h+1 primes, retained a3=3h+1 factorization, exact occupied domain P2(a3)={ell prime dividing a3:ell=2mod3} nonempty, and both original full/half radices. Codomain: The actual global least hit over each entire original radix, all original witness coordinates and the complete raw-scale fibre.

ell_star=min P2(a3) gives (A0,j,B0,epsilon)=(1,0,ell_star,0), global c=2(ell_star-1), D0=a3/ell_star,C0=(1+p ell_star)/3,u=a3 ell_star. Inverse: The original code decoder recovers the unique least digits, and D0,C0 plus retained k recover every full raw record. A boundary source (m+1,0,1,0) is recovered from its same-tag swap by the proved involution. Complete fibres: The least coprime record is unique; its full raw fibre is (k,k ell_star,k C0,D0/k^2,0) over all positive k^2|D0. If P2 is empty the complete j0 hit fibre is empty, without a claim on later j. Kernel: The exact occupation test is P2 nonempty, proved in both directions before the minimum is selected. No unproved universal occupation is assumed.

Original witness is (a3,C0D0,p ell_star C0D0), residuals D0 and p^2 ell_star^2D0. Comparing every A0>=2 and every later j>=1 proves the global first code; boundary swapped code2m is no smaller, with equality iff m+1=ell_star.

### R84 — Rational four-candidate action and every raw gcd scale

Evidence: `boundary_swap/boundary_swap_completion.tex:129` (`The raw-scale formulas are inverse because`).

Raw rational records and coprime records together with retained positive k satisfying k^2|D0; same original shell and tag.

(A0,B0,C0,D0,k)=(A/k,B/k,C/k,k^2D,k); inverse (kA0,kB0,kC0,D0/k^2). The gcd is recovered exactly, every scale occurs, and both bit operations commute with the same k,D0. The integral locus is equivalent by gcd(k,R)=1.

All original rational coefficients, signs, denominator coordinates and u remain recoverable. Forgetting k has the complete square-divisor fibre rather than silently discarding raw scale.

### R85 — Residual-seven integral gates and exact rational companions

Evidence: `boundary_swap/boundary_swap_completion.tex:481` (`For \(R=7\), the exterior same-tag integral restriction`).

Original R7 shell and exterior records, including p373,a95,(A,B,C,D)=(19,1,56,5),(5,1,54,19).

Same-tag swap is integral iff p=1 or6mod7; tag change iff p=1mod7. For p373 the exact swapped coefficients are7088/7 and1866/7, with original ordered witnesses (95,35440/7,251163280/7) and (95,35454/7,66121710/7). The rational inverse is the same bit involution.

The table proves exact denominator7 for each coefficient and both witness denominators, complete singleton integral orbits, and every retained failed rational companion. The explicit prime trial divisions and original reciprocal equation are checked.

## Independent verification runtime

Run `python proof_review/cyclic_incidence_checks.py` from the assigned research directory. It uses the already installed SymPy1.13.1 for integral Smith forms and complete modular square-root lists. The bundled dependency Python lacks SymPy; no installation was needed.

## Limits

- The audit covers the fifteen recorded proof inputs, including all explicitly reviewed corrections and continuation; it is not a claim about every source book or every earlier repository artifact.
- The full research objective, universal ES occupancy and universal prime coverage are outside this bounded compliance verdict.
- The original findings and their source hashes remain recorded separately from the final reviewed repairs. This stricter compliance verdict supplements the current theorem-correctness review.
